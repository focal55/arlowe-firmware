---
phase: 06-image-build-with-a-b-partitions
plan: 06
type: execute
wave: 4
depends_on: ["06-04", "06-05"]
files_modified:
  - .github/workflows/build-image.yml
  - .github/workflows/pr-checks.yml
  - scripts/flash-sd.sh
  - scripts/dev-deploy.sh
  - docs/operations/phase-6-build-flash-deploy.md
  - docs/operations/phase-6-repro-exceptions.md
autonomous: false

must_haves:
  truths:
    - "An arm64 CI workflow builds the image on tag + workflow_dispatch only (not on every PR)"
    - "PRs run shellcheck on the build/provision scripts (fast feedback)"
    - "flash-sd.sh writes a .img to a confirmed SD device; dev-deploy.sh rsyncs runtime/ to a target Pi and restarts affected units"
    - "Build inputs are pinned for best-effort reproducibility; residual nondeterminism is documented (no hash gate)"
    - "The on-hardware boot / A->B-flip / recovery / ready-to-pair verification is captured as a deferred hardware checkpoint with a runbook"
  artifacts:
    - path: ".github/workflows/build-image.yml"
      provides: "arm64 image build on tag + dispatch via pi-gen-action"
      contains: "ubuntu-24.04-arm"
    - path: "scripts/flash-sd.sh"
      provides: "guarded SD flashing"
      contains: "dd"
    - path: "scripts/dev-deploy.sh"
      provides: "rsync runtime/ + restart arlowe-* units over SSH"
      contains: "rsync"
  key_links:
    - from: ".github/workflows/build-image.yml"
      to: "scripts/build-image.sh"
      via: "CI invokes the build pipeline (or pi-gen-action wrapping it)"
      pattern: "build-image"
    - from: "scripts/dev-deploy.sh"
      to: "arlowe-* systemd units"
      via: "ssh systemctl restart of affected units"
      pattern: "systemctl restart"
---

<objective>
Ship the build/flash/deploy tooling and CI, lock in best-effort reproducibility, and stage the on-hardware verification as a deferred checkpoint (mirroring the Phase 1/3/4/5 precedent — arlowe-1 has no real arlowe layout and flashing a card + booting a Pi 5 needs the owner present).

CI: an arm64 image build on tag + workflow_dispatch only (full builds are slow/large — they must not gate PRs); PRs get a fast shellcheck job over the build/provision scripts. flash-sd.sh and dev-deploy.sh are the human-facing iteration tools. Reproducibility is input-pinning + a documented exception list, no hash gate (ADR-0004/SC5 reframe from plan 06-01).

Purpose: This closes the build half of the phase and hands the on-Pi verification to a hardware checkpoint, consistent with prior phases. Covers IMAGE-03, IMAGE-05 (flash), IMAGE-06, and the SC1/SC4 doc + hardware-run deferral.
Output: build-image CI workflow, PR shellcheck job, flash-sd.sh, dev-deploy.sh, build/flash/deploy + repro-exceptions docs, hardware checkpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-image-build-with-a-b-partitions/06-CONTEXT.md
@.planning/phases/06-image-build-with-a-b-partitions/06-RESEARCH.md
@.github/workflows/pr-checks.yml
@.github/workflows/sanitize.yml
@scripts/dev-pull-from-pi.sh
@docs/operations/phase-5-audio.md

# Wave-2 (06-04) + Wave-3 (06-05) outputs this plan exercises:
@.planning/phases/06-image-build-with-a-b-partitions/06-04-SUMMARY.md
@.planning/phases/06-image-build-with-a-b-partitions/06-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: arm64 image-build CI + PR shellcheck job + repro pinning</name>
  <files>.github/workflows/build-image.yml, .github/workflows/pr-checks.yml, docs/operations/phase-6-repro-exceptions.md</files>
  <action>
.github/workflows/build-image.yml: full image build, triggered ONLY on `push: tags: ['v*']` and `workflow_dispatch` (per CONTEXT cadence — NOT on PR). runs-on: ubuntu-24.04-arm (GitHub-hosted arm64, GA in private repos per 06-RESEARCH). Use usimd/pi-gen-action with: pi-gen-version=arm64 branch, the pi-gen/config + stage-arlowe from plan 06-03, release=bookworm, use-qcow2=true (the single most likely CI failure mode is disk headroom for a 32 GB target — enable qcow2 + free runner disk first; 06-RESEARCH flags this). The axcl deb + model artifacts come from secrets/cache ($AXCL_DEB, models cache) — wire them as the action expects; verify-third-party.sh gates inside build-image.sh. After build, run the repartition/clone/recovery/sanitize post-process (invoke scripts/build-image.sh, or a documented sequence if pi-gen-action only produces the rootfs). Upload the .img as an artifact. Add a clearly-marked fallback note: if the 32 GB qcow2 build does not fit the hosted runner, the manual arlowe-1 build (build-docker.sh) becomes primary — document, do not silently fail.

.github/workflows/pr-checks.yml: ADD a shellcheck job (PRs only) that runs shellcheck over scripts/**.sh, pi-gen/stage-arlowe/**/*.sh, units/install-units.sh, runtime/cli/arlowe-ab, runtime/recovery/*.sh. Keep it fast. Do NOT add the image build to PRs. Preserve the existing jobs.

docs/operations/phase-6-repro-exceptions.md: document the best-effort reproducibility posture (IMAGE-03 reframe per plan 06-01): inputs pinned = Debian snapshot date (pick one, e.g. snapshot.debian.org templated into apt sources), SOURCE_DATE_EPOCH set, axcl SHA-pinned, ax-llm submodule pinned, models SHA-pinned, in-chroot nondeterminism stripped (logs/machine-id/apt-cache/__pycache__/host-keys). Document the residual exception list that defeats bit-for-bit hashing (ext4 inode/timestamp internals, dpkg ordering, first-boot-regenerated material). State plainly: NO image-hash CI gate in v1.
  </action>
  <verify>grep -q "ubuntu-24.04-arm" .github/workflows/build-image.yml; grep -q "workflow_dispatch" .github/workflows/build-image.yml; grep -qi "qcow2" .github/workflows/build-image.yml; grep -qi "shellcheck" .github/workflows/pr-checks.yml; test -f docs/operations/phase-6-repro-exceptions.md; ! grep -q "pull_request" .github/workflows/build-image.yml</verify>
  <done>Image build runs on tag+dispatch on arm64 with qcow2; PRs gain a shellcheck job (no image build); repro posture + exception list documented, no hash gate.</done>
</task>

<task type="auto">
  <name>Task 2: flash-sd.sh + dev-deploy.sh + build/flash/deploy docs</name>
  <files>scripts/flash-sd.sh, scripts/dev-deploy.sh, docs/operations/phase-6-build-flash-deploy.md</files>
  <action>
scripts/flash-sd.sh: write a .img to a connected SD card (IMAGE-05). Require an explicit target device arg; confirm it is a removable disk and NOT the system disk before writing (boundary validation — refuse system disk). Use dd (or bmaptool if available, with dd fallback). Print device, size, and a confirmation prompt (or require --yes). Measure + print flash time (feeds the IMAGE-04 flash-time doc). set -euo pipefail, shellcheck-clean. This runs on macOS or Linux (the dev's flashing host) — note the Mac CAN flash even though it cannot BUILD.

scripts/dev-deploy.sh: rsync runtime/ over SSH to a target (default arlowe-1, overridable), then restart ONLY the affected arlowe-* units (IMAGE-06). Mirror scripts/dev-pull-from-pi.sh's SSH/target conventions (read it for host/alias handling). Detect which units to restart from what changed (or accept a units arg; restart arlowe-face/arlowe-voice/arlowe-dashboard/whisper-stt/qwen-* as relevant). ssh systemctl restart. Do NOT reflash. shellcheck-clean.

docs/operations/phase-6-build-flash-deploy.md: the operator runbook — how to build (arm64 host requirement, env vars: $AXCL_DEB, $ARLOWE_MODELS_DIR, $CARD_SIZE_GB; Mac-unsupported-for-build note + the arlowe-1 build-docker fallback), how to flash (flash-sd.sh, measured flash time — fill in from a real run or mark TODO for the hardware checkpoint), how to dev-deploy. Fill the flash-time TODO left in docs/operations/phase-6-partitions.md (plan 06-04) once measured, or cross-reference.
  </action>
  <verify>bash -n scripts/flash-sd.sh; bash -n scripts/dev-deploy.sh; shellcheck scripts/flash-sd.sh scripts/dev-deploy.sh || true; grep -q "dd\|bmaptool" scripts/flash-sd.sh; grep -q "rsync" scripts/dev-deploy.sh; grep -q "systemctl restart" scripts/dev-deploy.sh; test -f docs/operations/phase-6-build-flash-deploy.md</verify>
  <done>flash-sd.sh writes to a confirmed removable SD (refuses system disk, times the flash); dev-deploy.sh rsyncs runtime/ + restarts affected units over SSH; operator runbook documents build/flash/deploy.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
The full Phase 6 image pipeline: build-image.sh produces a 4-partition A/B .img (slot A provisioned, slot B recovery stub, owner-state grow-to-fill, tryboot root= selector), flash-sd.sh writes it, dev-deploy.sh iterates. CI builds on arm64 (tag+dispatch). All scripts pass shellcheck; the sanitize scan-dir gate passes against the real image.

On-hardware verification is deferred to this checkpoint (mirrors Phase 1/3/4/5 precedent: arlowe-1 has no real arlowe layout, and flashing a card + booting a real Pi 5 + WhisPlay needs the owner present). The runbook in docs/operations/phase-6-build-flash-deploy.md + phase-6-ab-recovery.md is the procedure.
  </what-built>
  <how-to-verify>
On a real Pi 5 + AX accelerator + WhisPlay, with a 32 GB+ SD card, owner present:

1. SC1 — Build + flash + boot to ready-to-pair:
   - Run scripts/build-image.sh on a supported arm64 host (or download the CI artifact). Confirm a .img is produced and the sanitize scan-dir gate passed.
   - scripts/flash-sd.sh <img> <device> to the 32 GB card. Note the flash time (record it in the docs, replacing the TODO).
   - Boot the Pi 5. Confirm: it reaches a "ready to pair" state — /etc/arlowe/config.yml ABSENT, the armed first-boot hook ran (journal shows "ready to pair"), no founder identity anywhere.

2. SC2 — Four partitions, sized, owner-state grown:
   - `lsblk` / `parted -s print`: confirm 4 partitions (/boot FAT, system A ext4, system B ext4 EQUAL size, /var/lib/arlowe ext4). Confirm owner-state grew to fill the 32 GB card on first boot and is mounted noatime. Confirm sizes match docs/operations/phase-6-partitions.md.

3. SC3 — A/B flip + recovery:
   - `arlowe-ab status` shows default = A. `arlowe-ab switch B`; on reboot confirm slot B boots the recovery rootfs: WhisPlay shows a recovery face (hardware alive, not dark/bricked), serial/journal print recovery state, the default is reset to A. Reboot again -> self-heals back to slot A. (If it freezes on a bad kernel, power-cycle per the documented Pi-5 caveat.)

4. SC4 — dev-deploy + recovery-SD doc:
   - From the dev host, `scripts/dev-deploy.sh` rsyncs runtime/ to the Pi and restarts affected arlowe-* units without reflashing; confirm a runtime change lands. Confirm the recovery-SD-card fallback procedure (PART-06) is documented and followable.

Also resolves the carried-over Phase 4 SC4 (on-device persona-knob end-to-end) if you exercise the config overlay on this real image, and the deferred Phase 5 on-Pi SC1-4 audio run if the WhisPlay/WM8960 audio path is exercised — note results in the respective runbooks.

Record results (pass/fail per SC, flash time, partition sizes, recovery behavior) in docs/operations/phase-6-build-flash-deploy.md.
  </how-to-verify>
  <resume-signal>Type "approved" (with the recorded SC1-SC4 results + flash time) or describe what failed.</resume-signal>
</task>

</tasks>

<verification>
- build-image.yml: arm64, tag+dispatch only, qcow2, no PR trigger.
- pr-checks.yml: shellcheck job added, image build NOT on PRs.
- flash-sd.sh refuses the system disk and times the flash; dev-deploy.sh rsyncs + restarts units.
- Repro exceptions + build/flash/deploy runbooks exist.
- Hardware checkpoint defers on-Pi SC1-SC4 with a concrete runbook (Phase 1/3/4/5 precedent).
- All scripts pass bash -n + shellcheck.
</verification>

<success_criteria>
The image is buildable in CI (arm64, tag/dispatch) and locally, flashable via flash-sd.sh, and iterable via dev-deploy.sh, with documented best-effort reproducibility. On-hardware SC1-SC4 are framed as a deferred owner-present checkpoint with a runbook. Covers IMAGE-03, IMAGE-05 (flash), IMAGE-06; SC1/SC4 on-Pi run deferred consistent with prior phases.
</success_criteria>

<output>
After completion, create `.planning/phases/06-image-build-with-a-b-partitions/06-06-SUMMARY.md`
</output>
