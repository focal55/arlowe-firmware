---
phase: 06-image-build-with-a-b-partitions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - pi-gen/config
  - pi-gen/stage-arlowe/00-packages-nr
  - pi-gen/stage-arlowe/00-run.sh
  - pi-gen/stage-arlowe/01-runtime/00-run.sh
  - pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh
  - pi-gen/stage-arlowe/02-models/00-run.sh
  - pi-gen/stage-arlowe/03-firstboot/00-run.sh
  - pi-gen/stage-arlowe/03-firstboot/files/arlowe-firstboot.service
  - pi-gen/stage-arlowe/EXPORT_NOOBS
  - runtime/face/README.md
autonomous: true

must_haves:
  truths:
    - "A custom pi-gen stage-arlowe assembles a single bootable arlowe rootfs (Pi OS Lite arm64 base)"
    - "The existing install-arlowe-*.sh + install-units.sh provisioning runs inside the chroot in the correct order, producing the Phase 3 fs layout, the arlowe user, units, CLI, udev/polkit, and config"
    - "The axcl deb is dpkg-installed and the WhisPlay driver is vendored to /opt/arlowe/third_party/whisplay-driver/ in the chroot"
    - "Models are staged into /opt/arlowe/models/ matching the unit-file paths"
    - "A first-boot hook is armed (stub) and the config overlay is absent (recognized 'not yet paired' state)"
  artifacts:
    - path: "pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh"
      provides: "ordered invocation of the existing provisioning scripts inside the rootfs"
      contains: "install-arlowe"
    - path: "pi-gen/stage-arlowe/03-firstboot/files/arlowe-firstboot.service"
      provides: "armed first-boot oneshot (owner-state grow + pairing-ready stub)"
      contains: "oneshot"
  key_links:
    - from: "pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh"
      to: "scripts/provision/install-arlowe-fs.sh"
      via: "runs the staged repo's provisioning scripts in order"
      pattern: "install-arlowe-fs"
---

<objective>
Build the pi-gen customization that turns Pi OS Lite arm64 into a single bootable arlowe rootfs — packages, the repo staged into the chroot, the existing provisioning scripts run in order, axcl installed, the WhisPlay driver vendored, models staged, and a first-boot hook armed. This is the slot-A content. It does NOT yet repartition or create A/B (that is plan 06-04); iterate with SKIP_IMAGES for speed.

Reuse, do NOT duplicate, the existing scripts/provision/install-arlowe-*.sh, units/install-units.sh, and provision/{udev,polkit}/*.rules. The composition reference is scripts/provision/install-arlowe-on-arlowe1-staging.sh — mirror its ORDER and prereqs, minus the -staging sed transforms (the image uses the literal `arlowe`).

Purpose: A self-contained, provisioned rootfs is the foundation everything else clones, partitions, and verifies. Getting reuse right here means Phase 9 OTA and future image rebuilds inherit the same provisioning, no drift.
Output: pi-gen/ config + stage-arlowe/ tree.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/06-image-build-with-a-b-partitions/06-CONTEXT.md
@.planning/phases/06-image-build-with-a-b-partitions/06-RESEARCH.md
@scripts/provision/install-arlowe-on-arlowe1-staging.sh
@scripts/provision/install-arlowe-user.sh
@scripts/provision/install-arlowe-fs.sh
@scripts/provision/install-arlowe-config.sh
@scripts/provision/install-arlowe-cli.sh
@scripts/provision/install-arlowe-udev-polkit.sh
@scripts/provision/extract-axcl-udev-from-deb.sh
@units/install-units.sh
@third_party/whisplay-driver/PROVENANCE.md
@.planning/todos/pending/F2-vendor-whisplay-driver.md
@config/defaults.yml
</context>

<tasks>

<task type="auto">
  <name>Task 1: pi-gen config + stage-arlowe scaffold + packages + repo staging</name>
  <files>pi-gen/config, pi-gen/stage-arlowe/00-packages-nr, pi-gen/stage-arlowe/00-run.sh, pi-gen/stage-arlowe/01-runtime/00-run.sh, pi-gen/stage-arlowe/EXPORT_NOOBS</files>
  <action>
Create the pi-gen customization tree under pi-gen/ in the repo (we vendor the stage, not pi-gen itself — pi-gen is pulled by the action/build script at the arm64 branch).

pi-gen/config: set IMG_NAME=arlowe, RELEASE=bookworm, TARGET_HOSTNAME placeholder (sanitize contract: NOT a founder hostname — use "arlowe"), ENABLE_SSH per project norms, STAGE_LIST="stage0 stage1 stage2 stage-arlowe" (stop at Lite; SKIP stage3-5), and the arm64-branch note in a comment. Reference 06-RESEARCH pi-gen mechanics.

pi-gen/stage-arlowe/00-packages-nr: apt deps (--no-install-recommends): python3 python3-venv python3-pip alsa-utils network-manager python3-rpi.gpio python3-spidev ripgrep (required by sanitize check.sh in plan 06-04) plus axcl deb prerequisites and node (for the dashboard) per runtime needs. Keep the list minimal — Lite base.

pi-gen/stage-arlowe/00-run.sh: host-side stage entry (standard pi-gen empty/no-op or marker as needed).

pi-gen/stage-arlowe/01-runtime/00-run.sh: host-side — copy the repo subdirs the chroot provisioning needs into the stage's files/ staging so they land at a known REPO_ROOT inside the rootfs (the install scripts resolve REPO_ROOT relative to their own location, so stage them as a coherent tree, e.g. /tmp/arlowe-build/<repo> in-chroot). Stage: scripts/provision/, units/, config/, runtime/, provision/, third_party/axcl/ (deb supplied via verify-third-party, copied from $AXCL_DEB), third_party/whisplay-driver/, scripts/verify-third-party.sh. Use pi-gen's on_chroot / files copy idioms.

EXPORT_NOOBS / EXPORT_IMAGE: do NOT emit a NOOBS image; we post-process to A/B in plan 06-04. Set up so iterative builds can use SKIP_IMAGES (document the flag in a comment).
  </action>
  <verify>test -f pi-gen/config; grep -q "stage-arlowe" pi-gen/config; test -f pi-gen/stage-arlowe/00-packages-nr; grep -q "ripgrep" pi-gen/stage-arlowe/00-packages-nr; bash -n pi-gen/stage-arlowe/01-runtime/00-run.sh</verify>
  <done>pi-gen config selects Lite arm64 + stage-arlowe; packages list includes runtime + ripgrep; 01-runtime stages the repo tree into the chroot.</done>
</task>

<task type="auto">
  <name>Task 2: chroot provisioning — run existing install scripts in order + axcl + whisplay</name>
  <files>pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh, runtime/face/README.md</files>
  <action>
Write 01-runtime/00-run-chroot.sh to run INSIDE the rootfs chroot. Mirror the ORDER in install-arlowe-on-arlowe1-staging.sh, using the LITERAL `arlowe` (no sed transforms). From the staged REPO_ROOT in-chroot:

1. install-arlowe-user.sh   (creates arlowe system user/group)
2. install-arlowe-fs.sh     (/opt/arlowe, /var/lib/arlowe, /etc/arlowe layout). NOTE its header: in the image /var/lib/arlowe is a separate partition mounted at first boot — here in the chroot it is a plain dir; install -d behaves identically, so running it in-chroot is correct. Do NOT create /etc/arlowe/config.yml (absence = CONFIG-03 pairing trigger).
3. install-arlowe-config.sh (ships schema.yml + defaults.yml + loader to /opt/arlowe/config)
4. units/install-units.sh   (copies *.service; it already skips daemon-reload when systemd is not PID1 — chroot-safe)
5. install-arlowe-cli.sh    (symlinks /usr/local/sbin/arlowe-* into /opt/arlowe/runtime/cli)
6. install-arlowe-udev-polkit.sh
7. extract-axcl-udev-from-deb.sh  (after the axcl deb is present)

Then:
- Copy runtime/, config/, etc. into /opt/arlowe (the cli symlink targets must resolve — install-arlowe-cli only creates links; populate /opt/arlowe/runtime/cli from the staged repo).
- dpkg -i the axcl deb (staged from $AXCL_DEB via verify-third-party); then run the axcl udev extraction.
- Vendor WhisPlay: copy third_party/whisplay-driver/WhisPlay.py + LICENSE + README to /opt/arlowe/third_party/whisplay-driver/ (F2). face.py default ARLOWE_WHISPLAY_DRIVER_PATH already points there — no env override needed on the image.
- Run the WM8960 audio-HAT install step ONLY IF the driver bundle is present (rights resolved); otherwise skip with a logged note (rights unresolved per PROVENANCE.md — plan 06-02 made this fetch-at-build/warn).
- Clean obvious nondeterminism in the chroot for repro (per 06-RESEARCH): clear apt caches, __pycache__, machine-id, ssh host keys (regenerated first boot). (Snapshot/SOURCE_DATE_EPOCH wiring belongs to plan 06-06's build orchestration; here just the in-chroot cleanup.)

Update runtime/face/README.md (F2 acceptance): note production images ship the vendored driver at the default path; ARLOWE_WHISPLAY_DRIVER_PATH override is only for dev environments.

Keep the script shellcheck-clean and set -euo pipefail.
  </action>
  <verify>bash -n pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh; shellcheck pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh || true; grep -q "install-arlowe-user" pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh; grep -q "install-arlowe-fs" pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh; grep -q "whisplay-driver" pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh; grep -qi "whisplay" runtime/face/README.md</verify>
  <done>Chroot script runs all seven provisioning scripts in order with literal arlowe, installs axcl, vendors WhisPlay, populates /opt/arlowe; face README notes the vendored default.</done>
</task>

<task type="auto">
  <name>Task 3: models staging + armed first-boot hook</name>
  <files>pi-gen/stage-arlowe/02-models/00-run.sh, pi-gen/stage-arlowe/03-firstboot/00-run.sh, pi-gen/stage-arlowe/03-firstboot/files/arlowe-firstboot.service</files>
  <action>
02-models/00-run.sh: stage the verified model artifacts (from plan 06-02's manifest/verify) into the chroot at the exact paths the units expect:
  - /opt/arlowe/models/qwen2.5-7b-int4-ax650/   (units/qwen-api.service QWEN_MODEL_DIR)
  - /opt/arlowe/models/whisper/small.en/        (or the path runtime/stt expects — confirm)
  - /opt/arlowe/models/piper-voices/en_US-lessac-medium.onnx + .onnx.json  (units/arlowe-voice.service ARLOWE_PIPER_MODEL)
Models are copied from the build-host cache (never committed). Fail clearly if absent (verify-third-party should already have gated this upstream).

03-firstboot: arm a first-boot hook WITHOUT pulling Phase 8 pairing scope forward. Create:
  - files/arlowe-firstboot.service: a systemd oneshot, WantedBy=multi-user.target, that runs a first-boot script ONCE (guarded by a sentinel file under /var/lib/arlowe). In v1 this stub: (a) is the hook the owner-state grow-to-fill resize hooks into (plan 06-04 wires the actual resize2fs), (b) confirms /etc/arlowe/config.yml is ABSENT so the device is in the recognized "not yet paired" state (from Phase 4), and (c) logs "ready to pair" to the journal. It does NOT start a pairing daemon (Phase 8). Enable the unit in the chroot (systemctl enable, or symlink under the wants dir since systemd isn't PID1).
  - 03-firstboot/00-run.sh: install the service + its script into the chroot and enable it.

Document in a comment that the pairing daemon itself is Phase 8; this hook only brings the image to "armed / ready to pair".
  </action>
  <verify>bash -n pi-gen/stage-arlowe/02-models/00-run.sh; grep -q "qwen2.5-7b-int4-ax650" pi-gen/stage-arlowe/02-models/00-run.sh; grep -q "piper" pi-gen/stage-arlowe/02-models/00-run.sh; test -f pi-gen/stage-arlowe/03-firstboot/files/arlowe-firstboot.service; grep -qi "oneshot" pi-gen/stage-arlowe/03-firstboot/files/arlowe-firstboot.service</verify>
  <done>Models stage to the unit-expected paths; an armed first-boot oneshot exists, asserts config-absent, logs ready-to-pair, and is the seam for plan 06-04's resize — without Phase 8 pairing logic.</done>
</task>

</tasks>

<verification>
- pi-gen/stage-arlowe/ tree exists with config, packages, runtime, models, firstboot sub-stages.
- The chroot script invokes the existing provisioning scripts in staging-reference order, literal arlowe.
- WhisPlay vendored to /opt/arlowe/third_party/whisplay-driver/; runtime/face/README.md updated.
- Model staging targets match units/qwen-api.service, units/arlowe-voice.service, runtime/stt.
- First-boot oneshot is armed and config-overlay-absent is asserted (ready-to-pair, no Phase 8 daemon).
- All new shell scripts pass bash -n and shellcheck.
</verification>

<success_criteria>
A pi-gen build through stage-arlowe (SKIP_IMAGES for iteration) yields a single arlowe rootfs that, if booted, has the full Phase 3 fs layout, the arlowe user, units, CLI, udev/polkit, config defaults, axcl, vendored WhisPlay, staged models, and an armed first-boot hook — with no founder identity and no config overlay (ready to pair). Covers IMAGE-02.
</success_criteria>

<output>
After completion, create `.planning/phases/06-image-build-with-a-b-partitions/06-03-SUMMARY.md`
</output>
