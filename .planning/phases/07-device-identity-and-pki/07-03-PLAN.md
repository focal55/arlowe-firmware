---
phase: 07-device-identity-and-pki
plan: 03
type: tdd
wave: 2
depends_on: ["07-02"]
files_modified:
  - runtime/lib/arlowe_identity.py
  - runtime/lib/tests/test_arlowe_identity.py
  - runtime/lib/tests/fixtures/identity/**
  - scripts/provision/install-arlowe-config.sh
autonomous: true

must_haves:
  truths:
    - "A device with no network derives a device-unique ID and persists it to /var/lib/arlowe/identity/device-id"
    - "Re-running derivation returns the same ID; the ID is never re-derived once persisted"
    - "device-id and device-entropy are mode 0600 even under a umask of 0022"
    - "The resolved hostname can never contain the string that the sanitize banlist matches"
    - "Which serial source was used is recorded, so a field failure is diagnosable"
    - "identity.json can be updated with new fields after creation without weakening the write-once guarantee on any other file"
    - "An update to identity.json is atomic: a power cut during it leaves either the old file or the new one, never neither"
  artifacts:
    - path: "runtime/lib/arlowe_identity.py"
      provides: "device-id derivation, entropy generation, identity-store path constants, hostname resolution"
      exports: ["IDENTITY_DIR", "DEVICE_ID_PATH", "ENTROPY_PATH", "KEY_PATH", "CSR_PATH", "CERT_PATH", "METADATA_PATH", "read_serial", "ensure_entropy", "derive_device_id", "ensure_device_id", "resolve_hostname", "write_secret", "read_metadata", "update_metadata"]
      min_lines: 110
    - path: "runtime/lib/tests/test_arlowe_identity.py"
      provides: "offline unit tests with fixture serial sources"
      min_lines: 140
  key_links:
    - from: "runtime/lib/arlowe_identity.py"
      to: "/var/lib/arlowe/identity"
      via: "ARLOWE_IDENTITY_DIR env override, mirroring arlowe_config's ARLOWE_*_PATH pattern"
      pattern: "ARLOWE_IDENTITY_DIR"
    - from: "runtime/lib/arlowe_identity.py"
      to: "os.open O_EXCL 0o600"
      via: "write_secret, the single writer for every write-once file in the identity store (identity.json is the documented exception, owned by update_metadata)"
      pattern: "O_EXCL"
    - from: "runtime/lib/arlowe_identity.py"
      to: "/var/lib/arlowe/identity/identity.json"
      via: "read_metadata / update_metadata — the ONLY sanctioned mutators of the metadata file"
      pattern: "def update_metadata"
    - from: "scripts/provision/install-arlowe-config.sh"
      to: "/opt/arlowe/runtime/lib/"
      via: "installs every runtime/lib/*.py module"
      pattern: "runtime/lib"
---

<objective>
Build `arlowe_identity.py`: the device-unique ID derivation, the per-device entropy, the identity
store path contract, and the one function every later plan uses to write a secret at 0600.

Purpose: this is the derivation half of SC2 and all of IDENT-06. It is the foundation the CSR
(07-06), the cloud client (07-07) and the CLI (07-08a) all import, so its path constants, its
secret-writing primitive, and the `identity.json` read/update contract are defined here once —
in one place, because three plans mutate or read that file.
Output: a fully offline-testable module plus the installer change that ships it.

This is a TDD plan: write the failing tests first, then the module.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-device-identity-and-pki/07-RESEARCH.md
@.planning/phases/07-device-identity-and-pki/07-02-SUMMARY.md
@runtime/lib/arlowe_config.py
@runtime/lib/tests/test_arlowe_config.py
@scripts/provision/install-arlowe-config.sh
@scripts/sanitize/banlist.txt
@config/defaults.yml
</context>

<feature>
  <name>Device-unique ID derivation and the identity store contract</name>
  <files>runtime/lib/arlowe_identity.py, runtime/lib/tests/test_arlowe_identity.py</files>

  <behavior>
Design constants. Every later plan depends on these exactly as written.

```
IDENTITY_DIR   = Path(os.environ.get("ARLOWE_IDENTITY_DIR", "/var/lib/arlowe/identity"))
DEVICE_ID_PATH = IDENTITY_DIR / "device-id"
ENTROPY_PATH   = IDENTITY_DIR / "device-entropy"
KEY_PATH       = IDENTITY_DIR / "device.key"
CSR_PATH       = IDENTITY_DIR / "device.csr"
CERT_PATH      = IDENTITY_DIR / "device.crt"
METADATA_PATH  = IDENTITY_DIR / "identity.json"
SECRET_MODE    = 0o600
```

The env-override pattern mirrors `arlowe_config.py`'s `ARLOWE_*_PATH` constants, which is what
makes this module testable offline with fixture paths. Note the paths are read at import time, so
tests must monkeypatch the module attributes as well as the env var — copy the `_set_paths` idiom
from `test_arlowe_config.py`.

**`write_secret(path, data: bytes) -> None`**
The single writer for every write-once file in the identity store: `device-id`, `device-entropy`,
`device.key`, `device.csr`, `device.crt`. `identity.json` is the one documented exception and is
owned by `update_metadata` (below) — nothing else may bypass it.
- `fd = os.open(path, os.O_WRONLY | os.O_CREAT | os.O_EXCL, SECRET_MODE)`, then `os.fdopen(fd,"wb")`.
- `O_EXCL` means it raises `FileExistsError` on an existing path. That is deliberate: identity
  material is written once. Callers that need replacement must remove first and say so.
- Follow up with an explicit `os.chmod(path, SECRET_MODE)`. `os.open`'s mode argument is masked by
  the process umask, so under systemd's default `UMask=0022` an `O_CREAT` with `0o600` still lands
  at `0o600` (0600 & ~0022 == 0600) but under a more permissive umask assumption this is the belt
  to the O_EXCL braces. The explicit chmod makes the mode independent of umask entirely.
- Tests must prove this under a hostile umask: `os.umask(0o000)` in the test, write, assert
  `stat.S_IMODE(path.stat().st_mode) == 0o600`.

**`read_serial() -> tuple[str, str]` returning `(source_tag, serial)`**
Try these in order and return the first that yields a non-empty value:
| order | path | source_tag |
|---|---|---|
| 1 | `/proc/device-tree/chosen/rpi-duid` | `rpi-duid` |
| 2 | `/sys/firmware/devicetree/base/serial-number` | `dt-serial` |
| 3 | `/proc/cpuinfo`, the `Serial` field | `cpuinfo` |
`rpi-duid` is preferred because it is factory-allocated rather than RNG-derived and matches the 2D
data-matrix laser-etched on the PCB, which matters for RMA and support.
Device-tree reads are NUL-terminated: strip trailing `\x00` and whitespace. Raise
`IdentitySourceUnavailable` (a module-level exception) if all three are unavailable — that is a
boundary condition on real hardware, so it validates loudly rather than defaulting.
The search roots must be overridable for tests (module-level `SERIAL_SOURCES` list of
`(source_tag, Path)` built from an `ARLOWE_SERIAL_ROOT` prefix, default `/`).

**`ensure_entropy() -> bytes`**
- If `ENTROPY_PATH` exists, read and return its 32 bytes.
- Otherwise generate `os.urandom(32)` exactly once and `write_secret` it.
- This is per-device entropy. It is NOT a TPM and NOT hardware-backed. Its purposes are:
  (a) closing the documented Pi-4-era duplicate-serial hole, (b) guaranteeing two units with the
  same serial cannot collide, (c) making the ID non-guessable from the serial alone so it is not an
  enumeration vector. Put that reasoning in the docstring — it is the non-obvious WHY.

**`derive_device_id(source_tag, serial, entropy) -> str`**
`sha256(f"{source_tag}:{serial}:{entropy.hex()}".encode()).hexdigest()[:32]` — a 32-hex-char opaque
token. Pure function, no I/O, trivially testable with known vectors.

**`ensure_device_id() -> str`**
Idempotent and stable. If `DEVICE_ID_PATH` exists, read and return it — **never re-derive**. Else
read the serial, ensure entropy, derive, `write_secret` the ID, and create `METADATA_PATH` via
`update_metadata` (below — there must be exactly one code path that writes that file) recording `{"device_id", "serial_source", "derivation_version": 1,
"derived_at": <ISO-8601 UTC>}`. Do NOT record the raw serial in metadata — the device-id is the
opaque token by design.
Both the entropy file and the ID live on `/var/lib/arlowe`, which is a dedicated ext4 partition
(p4, owner_state) shared across A/B slots. So the ID survives a slot flip and an app OTA, and is
destroyed by factory reset (PAIR-07). That lifecycle is correct — document it.

**`read_metadata() -> dict`**
Parse `METADATA_PATH` and return it. Returns `{}` if the file does not exist. Raises
`ValueError` on unparseable JSON rather than silently returning `{}` — a corrupt identity.json on a
provisioned device is a real fault and swallowing it produces a device that looks unprovisioned.
Plan 07-07's `resolve_endpoints` reads through this function, never by opening the path itself.

**`update_metadata(**fields) -> dict`** — the ONLY sanctioned way to change `identity.json` after
creation. Returns the merged dict.

This exists because of a genuine conflict that would otherwise be discovered at execution time:
`write_secret` uses `O_EXCL` and `ensure_device_id` has already created `identity.json` through it,
so a later merge of provisioned fields into that same file (plan 07-08a's `provision`, step 4)
would raise `FileExistsError`. **The cheap-looking fix — dropping `O_EXCL` from `write_secret` — is
forbidden.** `O_EXCL` is the write-once guarantee on `device.key`, `device-entropy` and `device-id`;
weakening it to make a *metadata* update convenient would trade the load-bearing property of the
whole module for a one-line saving. So the update path is explicit, narrow, and lives here:

```python
def update_metadata(**fields):
    current = read_metadata()
    merged = {**current, **fields}
    data = json.dumps(merged, indent=2, sort_keys=True).encode()
    # identity.json is the ONE file in the store that is legitimately mutable: it is
    # bookkeeping (which serial source, which certificate id), not key material. Every
    # other file here is write-once and must stay that way -- do NOT generalise this
    # temp-and-replace into write_secret.
    tmp = METADATA_PATH.with_name(METADATA_PATH.name + ".tmp")
    tmp.unlink(missing_ok=True)
    fd = os.open(tmp, os.O_WRONLY | os.O_CREAT | os.O_EXCL, SECRET_MODE)
    try:
        with os.fdopen(fd, "wb") as fh:
            fh.write(data)
            fh.flush()
            os.fsync(fh.fileno())
        os.chmod(tmp, SECRET_MODE)          # explicit: os.open's mode arg is umask-masked
        os.replace(tmp, METADATA_PATH)      # atomic within the same directory
    except BaseException:
        tmp.unlink(missing_ok=True)
        raise
    return merged
```

**Write the temp file with `os.open(..., O_CREAT|O_EXCL, 0o600)` plus an explicit `os.chmod`, then
`os.replace`.** Do not use a naive `open(tmp, "w")` — *that* form does inherit the umask, and it is
the only form the umask objection applies to. The shape above keeps every property being protected:
`O_EXCL` on the temp path, an explicit chmod that makes the mode independent of umask exactly as
`write_secret` does, and `os.replace` within the same directory, which is atomic.

Atomicity is the point, and it is not decorative. An unlink-then-rewrite leaves a window in which
`identity.json` does not exist; a power cut inside that window **permanently** loses `serial_source`
and `derived_at`, because `ensure_device_id` never rewrites metadata once `device-id` exists — it
short-circuits on the persisted ID and returns. The device would boot with a valid device-id and no
record of how it was derived, which is precisely the field-diagnosability this module exists to
provide. `os.replace` closes that window: a reader sees either the old file or the new one, never
neither.

`write_secret` remains the single writer for every *write-once* file in the store — `device-id`,
`device-entropy`, `device.key`, `device.csr`, `device.crt`. `identity.json` is the documented
exception, and `update_metadata` is the only function permitted to take it. Say that in the
docstring, and say why the temp-and-replace is not a licence to weaken `O_EXCL` anywhere else.

Refactor `ensure_device_id` to create `identity.json` via `update_metadata` too, so there is exactly
one code path that writes that file.

**`resolve_hostname(template: str, device_id: str) -> str`**
`config.device.hostname` defaults to the template literal `arlowe-${device_serial}`. This function
substitutes it.

**The substitution value is `"d" + device_id[:12]`, not the raw ID.** This is not cosmetic.
`scripts/sanitize/check.sh` runs `rg -iFnH` against `scripts/sanitize/banlist.txt`, which contains
the literal `arlowe-1`. `-F` is fixed-string with no word boundaries and `-i` is case-insensitive,
so a hostname like `arlowe-1a2b3c4d` **contains** `arlowe-1` and trips the gate. Worse,
`--scan-dir` mode — the mode `scripts/build-image.sh` uses over the mounted slot-A rootfs — ignores
`.sanitize-allowlist` entirely, so there is no escape valve. The constant `d` prefix guarantees the
character immediately after `arlowe-` is a letter, and hex digits cannot spell any other banlist
entry. Result: `arlowe-d1a2b3c4d5e6`. Put the banlist reasoning in the docstring; a future
developer who "simplifies" this back to the raw ID must be told why not.

Raise `ValueError` if the template contains no `${device_serial}` placeholder.
  </behavior>

  <implementation>
Module lives flat at `runtime/lib/arlowe_identity.py`, matching the flat import convention
(`from arlowe_identity import ensure_device_id`) used by `arlowe_config.py` at
`/opt/arlowe/runtime/lib/`. Standard library only — no third-party imports in this module.

**RED**: write `runtime/lib/tests/test_arlowe_identity.py` first and confirm it fails.
Required cases:
- `read_serial` picks `rpi-duid` when all three fixture sources exist; falls back to `dt-serial`
  when rpi-duid is absent; falls back to `cpuinfo` when both are absent; raises
  `IdentitySourceUnavailable` when none exist.
- Device-tree NUL termination is stripped (`b"ABC123\x00"` -> `"ABC123"`).
- `derive_device_id` is deterministic for fixed inputs and is 32 lowercase hex chars.
- Two different entropy values with the SAME serial produce different IDs (the collision-hole test).
- Two different source_tags with the same serial+entropy produce different IDs.
- `ensure_entropy` generates once: call twice, assert identical bytes and that the file mtime /
  content did not change.
- `ensure_device_id` is idempotent: call twice, assert identical; then corrupt the entropy file and
  call again, asserting the ID is still the persisted one (proving it never re-derives).
- Modes: with `os.umask(0o000)` set, `device-id`, `device-entropy` and `identity.json` are all
  exactly `0o600`.
- `write_secret` raises `FileExistsError` on an existing path.
- `resolve_hostname` substitutes correctly and raises `ValueError` on a template with no placeholder.
- **Banlist property test**: generate 5000 device-ids from random 32-byte entropy, resolve each
  hostname, and assert for every banned literal in `scripts/sanitize/banlist.txt` that
  `literal.lower() not in hostname.lower()`. Read the banlist from the repo — do not hard-code the
  entries, so the test tracks the real gate.
  Resolve it **relative to the test file, not the cwd**:
  `Path(__file__).resolve().parents[3] / "scripts/sanitize/banlist.txt"`
  (`runtime/lib/tests/test_arlowe_identity.py` -> parents[3] is the repo root). A cwd-relative path
  makes the test pass or fail depending on where pytest was invoked from, and CI's `python-test`
  job runs from the repo root while a developer may not. Assert the file exists and skip loudly
  with a reason if it does not — a silently-empty banlist makes this property test vacuous.
- `read_metadata` returns `{}` for a missing file and raises `ValueError` on corrupt JSON.
- `update_metadata` merges into an `identity.json` that `ensure_device_id` already created —
  **this is the regression test for the `O_EXCL` collision**: call `ensure_device_id()`, then
  `update_metadata(certificate_id="abc")`, and assert no `FileExistsError`, that `device_id` and
  `serial_source` from the original write survive, and that `certificate_id` is present.
- `update_metadata` under `os.umask(0o000)` leaves `identity.json` at exactly `0o600`.
- `update_metadata` called on a store where `identity.json` does not exist creates it.
- `update_metadata` leaves no `identity.json.tmp` behind on success, and none on failure either:
  patch `os.replace` to raise and assert the temp file was cleaned up and the original
  `identity.json` is byte-identical to what it was before the call. This is the atomicity
  assertion — an implementation that unlinks the target first would fail it.
- The temp file is `0o600` at every point it exists: set `os.umask(0o000)`, patch `os.replace` to
  assert `stat.S_IMODE(os.stat(tmp).st_mode) == 0o600` before delegating to the real one.
- After `update_metadata`, `device-id`, `device-entropy` and (if present) `device.key` are
  untouched — assert `write_secret` still raises `FileExistsError` on `DEVICE_ID_PATH`, proving the
  write-once guarantee was not weakened to make the update work.

Fixtures go under `runtime/lib/tests/fixtures/identity/` as small serial-source trees
(`rpi_duid_only/`, `dt_serial_only/`, `cpuinfo_only/`, `all_three/`). Every directory that would
otherwise be empty needs a `.keep` — git does not track empty directories and a vanished fixture
directory is a known failure mode in this repo.

Do not commit a fixture containing a rendered hostname of the `arlowe-<hex>` shape; the sanitize
gate scans tracked files.

**GREEN**: implement `arlowe_identity.py` until the suite passes.

**Installer**: `scripts/provision/install-arlowe-config.sh` currently installs
`arlowe_config.py` and `arlowe_config_validate.py` by name. Replace those two explicit `install`
calls with a loop over `runtime/lib/*.py` installing each as `root:arlowe 0644` into
`/opt/arlowe/runtime/lib/`, and update the destination-layout comment block at the top of the file.
Reason, and put it in the comment: adding a module should not require editing the installer. The
repo has already been bitten once by an install list drifting from reality
(F7 #21, `install-arlowe-cli.sh`). Keep an explicit guard that fails if the glob matches nothing.
`runtime/lib/tests/` is a directory, so the `*.py` glob does not pick up test files.
  </implementation>
</feature>

<verification>
- `PYTHONPATH=runtime/lib python3 -m pytest runtime/lib/tests/test_arlowe_identity.py -q` passes.
- The full suite still passes: `PYTHONPATH=runtime/lib ARLOWE_SCHEMA_PATH=config/schema.yml ARLOWE_DEFAULTS_PATH=config/defaults.yml ARLOWE_CONFIG_PATH=/nonexistent python3 -m pytest runtime/lib/tests/ -q`.
- End-to-end on a temp dir with a hostile umask:
  `ARLOWE_IDENTITY_DIR=$(mktemp -d) ARLOWE_SERIAL_ROOT=runtime/lib/tests/fixtures/identity/all_three PYTHONPATH=runtime/lib python3 -c "import os,stat,arlowe_identity as a; os.umask(0); i=a.ensure_device_id(); assert i==a.ensure_device_id(); assert stat.S_IMODE(a.DEVICE_ID_PATH.stat().st_mode)==0o600; print(i, a.resolve_hostname('arlowe-\${device_serial}', i))"`
- The printed hostname does not contain `arlowe-1`.
- `bash scripts/sanitize/check.sh` exits 0.
- `bash -n scripts/provision/install-arlowe-config.sh` and `shellcheck scripts/provision/install-arlowe-config.sh` are clean.
- Net diff under **550** lines. **This is an honest number, not the 400 an earlier draft carried,
  and it was raised when `update_metadata` was added to this plan's scope.** The declared
  `min_lines` alone (110 + 140) floor it at 250; the realistic module lands near 200 with the eight
  mandated docstrings, and the test suite at 300-330 across roughly 22 required cases including the
  5000-iteration banlist property test. `pr-checks.yml` enforces a Phase-1-era cap of 1500 and will
  not catch an overrun, so the number here is the only guard — which is exactly why it must be
  truthful rather than aspirational.
  **Do not satisfy this number by removing test cases.** The two cheapest-looking cuts are the
  banlist property test and the `update_metadata` regression cases, and those are the two things
  this plan exists to guarantee: the property test is the only live check on Trap 2, and the
  `update_metadata` cases are the regression net for the `O_EXCL` collision. If the work genuinely
  exceeds 550, cut prose and docstring length first; if it still exceeds, stop and report rather
  than shipping a thinner suite.
</verification>

<success_criteria>
- SC2's derivation half is satisfied: a device with no network derives a device-unique ID from a real serial source plus per-device entropy and persists it to `/var/lib/arlowe/identity/device-id`.
- IDENT-06 is satisfied.
- Trap 2 (sanitize gate vs serial-derived hostname) is closed by construction and proved by a property test that reads the live banlist.
- Trap 4's code half is closed: `write_secret` is the only writer for every write-once file and it is umask-independent, and the one mutable file in the store (`identity.json`) is updated through a narrow, tested, **atomic** primitive (`O_EXCL` temp file + explicit chmod + `os.replace`) rather than by weakening `O_EXCL` or by an unlink that could lose `serial_source` to a power cut.
</success_criteria>

<output>
After completion, create `.planning/phases/07-device-identity-and-pki/07-03-SUMMARY.md`.
Record: the exported path constants verbatim (07-06/07-07/07-08a import them), the `write_secret`
contract including the `FileExistsError` on re-write, the **`read_metadata` / `update_metadata`
signatures and the rule that they are the only sanctioned readers/mutators of `identity.json`**
(07-07's `resolve_endpoints` and 07-08a's `provision` both go through them, and 07-08a's step 4
would raise `FileExistsError` without `update_metadata`), the hostname encoding rule and its
banlist reason, and the `ARLOWE_IDENTITY_DIR` / `ARLOWE_SERIAL_ROOT` test overrides.
</output>
