---
phase: 07.1-runtime-substrate-repair
plan: 04
type: execute
wave: 2
depends_on: ["07.1-01", "07.1-03"]
files_modified:
  - pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh
  - pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh
  - pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh
  - runtime/dashboard/next.config.ts
  - runtime/dashboard/package.json
  - units/arlowe-dashboard.service
  - scripts/provision/install-arlowe-fs.sh
  - .github/workflows/ci.yml
autonomous: true

must_haves:
  truths:
    - "/opt/arlowe/venvs/{voice,llm,stt}/bin/python exist in the built rootfs and are executable"
    - "/opt/arlowe/runtime/dashboard/server.js exists in the built rootfs"
    - "The Node binary the dashboard unit invokes satisfies next@16.1.6's >=20.9.0 engines floor, proven by plan 07.1-03's verify_unit_runtime_versions against the path the unit names — not by whatever happens to be on PATH at build time"
    - "The pnpm version is pinned in one place (package.json packageManager) and both the chroot build and CI derive from it"
    - "The build steps run at a point in the chroot script where their inputs already exist and before the staged repo tree is deleted"
    - "The dashboard's build-time node_modules do not ship in the image"
    - "install-arlowe-fs.sh no longer claims a deferral that has been closed"
  artifacts:
    - path: "pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh"
      provides: "creates the three --system-site-packages venvs and installs the pinned requirements"
      min_lines: 60
    - path: "pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh"
      provides: "pnpm install + next standalone build + relocation to the ExecStart path + build-artifact cleanup"
      min_lines: 60
  key_links:
    - from: "pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh"
      to: "pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh"
      via: "invoked after the runtime rsync and before the reproducibility cleanup deletes /root/arlowe-build"
      pattern: "build-venvs.sh"
    - from: "pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh"
      to: "pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh"
      via: "invoked after the runtime rsync populates /opt/arlowe/runtime/dashboard"
      pattern: "build-dashboard.sh"
    - from: "runtime/dashboard/next.config.ts"
      to: "/opt/arlowe/runtime/dashboard/server.js"
      via: "output: 'standalone' is what makes next build emit a server.js at all"
      pattern: "standalone"
    - from: "units/arlowe-dashboard.service"
      to: "the Node prefix chosen in ADR-0008"
      via: "ExecStart names the vendored interpreter explicitly; verify_unit_runtime_versions asserts its version floor"
      pattern: "ExecStart="
    - from: "runtime/dashboard/package.json"
      to: ".github/workflows/ci.yml"
      via: "packageManager field is the single pnpm pin; pnpm/action-setup derives from it instead of a floating `version: 10`"
      pattern: "packageManager"
---

<objective>
Make the chroot actually produce the artifacts the units invoke: the three venv interpreters and the
dashboard's `server.js`.

Purpose: `scripts/provision/install-arlowe-fs.sh:51-52` creates `/opt/arlowe/venvs` and says
*"venvs/ is empty in Phase 3; Phase 6 populates from runtime/*/requirements.txt"*. Phase 6 did not.
`grep -rn venv pi-gen/stage-arlowe/` returns only the apt package name. Separately,
`runtime/dashboard/next.config.ts` is the default stub with no `output` key, so Next never emits a
`server.js`, and no build step runs anywhere in the image pipeline. This plan closes both, in the
chroot, at a point in the script where the inputs exist.
Output: two chroot build scripts, their wiring, the Next config change, the corrected comment, and
the Node runtime decided in ADR-0008.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07.1-runtime-substrate-repair/07.1-01-SUMMARY.md
@docs/architecture/0008-image-runtime-dependency-strategy.md

@pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh
@pi-gen/stage-arlowe/01-runtime/00-run.sh
@pi-gen/stage-arlowe/03-firstboot/00-run-chroot.sh
@scripts/provision/install-arlowe-fs.sh
@runtime/dashboard/package.json
@runtime/dashboard/next.config.ts
@units/arlowe-dashboard.service
@units/arlowe-voice.service
</context>

<tasks>

<task type="auto">
  <name>Task 1: Populate the three venvs in the chroot</name>
  <files>pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh, pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh, scripts/provision/install-arlowe-fs.sh</files>
  <action>
Write `pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh`, run inside the chroot as root. For each
of `voice`, `llm`, `stt`:

  python3 -m venv --system-site-packages /opt/arlowe/venvs/<name>
  /opt/arlowe/venvs/<name>/bin/pip install --no-cache-dir \
      -r <staged>/venv-requirements/<name>.txt -c <staged>/venv-requirements/constraints.txt

`--system-site-packages` is the decision recorded in ADR-0008: it lets the venvs see the apt-supplied
compiled packages (numpy, Pillow, scipy, sklearn, joblib, PyAudio) instead of building them, which
is what makes the build work identically on the native-arm64 CI runner (`ubuntu-24.04-arm`) and on
arlowe-1. Do not change this without amending the ADR.

Then set ownership and permissions consistent with the rest of `/opt/arlowe`: root-owned, readable
and traversable by the `arlowe` group (`install-arlowe-fs.sh` uses `root:arlowe 0755` throughout).
The units run as `User=arlowe` under `ProtectSystem=strict`, so the interpreters must be executable
by that user and nothing under `/opt/arlowe/venvs` may need to be writable at runtime.

After install, assert in-script — do not defer this to the outer gate:
  - `/opt/arlowe/venvs/<name>/bin/python` exists and is executable
  - `pip check` reports no broken dependencies in each venv
  - `pip freeze --local` output is written to `/opt/arlowe/venvs/<name>/FROZEN.txt` and echoed to the
    build log. That file is the on-device record of what was installed; a support engineer reading a
    failed device should not have to guess.
Exit non-zero on any failure.

WIRING. Read `00-run-chroot.sh` before editing. Its step order matters and has a documented defect
history: CLI symlinks used to run before their targets existed, which made F7 #21 invisible until
hardware. Insert the call where inputs exist:
  - AFTER the `rsync -a --chown=root:arlowe "${REPO_ROOT}/runtime/" /opt/arlowe/runtime/` at ~line 95
    (build-venvs does not need it, but the dashboard build in Task 2 does, and keeping both new steps
    together keeps the order legible)
  - AFTER `step 6: install-arlowe-cli.sh` (~line 110)
  - BEFORE the reproducibility-cleanup block at the end, which does `rm -rf /root/arlowe-build`,
    `apt-get clean`, `rm -rf /var/lib/apt/lists/*` and a `__pycache__` purge. Running after any of
    those breaks the build: the requirement files live under the staged tree.
Renumber the step comments. The existing "step 7: extract-axcl-udev-from-deb.sh" comment must move to
whatever number it now occupies, and the header block at lines 9-16 must be updated to match. The
brief for this phase asks for this explicitly.

Note in a comment that the cleanup block's `find /opt/arlowe -name "__pycache__" -exec rm -rf` will
strip the venvs' bytecode caches. That is intended (determinism) and costs only first-start latency;
say so, so a later reader does not "fix" it.

The requirement files must reach the chroot. They live under
`pi-gen/stage-arlowe/01-runtime/files/`, which is NOT the staged repo tree — check how
`03-firstboot/00-run-chroot.sh` gets at `03-firstboot/files/` and follow the same mechanism. If
pi-gen does not expose the sub-stage `files/` dir inside the chroot, have the host-side
`00-run.sh` copy them into `/root/arlowe-build/` alongside the repo, and say which mechanism you
used in the SUMMARY.

Finally, correct `scripts/provision/install-arlowe-fs.sh:51`. It currently says Phase 6 populates the
venvs. Replace with a comment stating that `/opt/arlowe/venvs` is created empty here and populated by
`pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh` from the pinned requirement files, naming the
path. A stale deferral comment is how this gap survived two phases.
  </action>
  <verify>
Full chroot exercise in an arm64 container, without pi-gen. This is the honest test — the Mac's
Python is irrelevant here:

  docker run --rm --platform linux/arm64 -v "$PWD:/repo" debian:bookworm bash -c '
    set -e
    apt-get update -qq >/dev/null
    mapfile -t P < <(sed "s/#.*//" /repo/pi-gen/stage-arlowe/00-packages/00-packages-nr | tr -s "[:space:]" "\n" | grep -v "^$")
    apt-get install -y -qq --no-install-recommends "${P[@]}" >/dev/null
    groupadd --system arlowe && useradd --system -g arlowe -d /var/lib/arlowe -s /usr/sbin/nologin arlowe
    mkdir -p /opt/arlowe/venvs /root/arlowe-build
    cp -r /repo/pi-gen/stage-arlowe/01-runtime/files /root/arlowe-build/
    bash /repo/pi-gen/stage-arlowe/01-runtime/files/build-venvs.sh
    for v in voice llm stt; do
      test -x /opt/arlowe/venvs/$v/bin/python || { echo "MISSING $v"; exit 1; }
      echo "--- $v ---"; /opt/arlowe/venvs/$v/bin/pip check; head -5 /opt/arlowe/venvs/$v/FROZEN.txt
    done
    echo VENVS_OK
    du -sh /opt/arlowe/venvs'

Expected: `VENVS_OK` plus a size figure. Record the `du -sh` number in the SUMMARY — it feeds
directly into `build-image.sh`'s measure-then-set slot sizing (two slots carry it) and therefore into
whether the 16 GB card remains viable under ADR-0004. If the venvs add more than ~500 MiB, flag it
explicitly as a card-sizing consequence rather than letting the next hardware checkpoint discover it.
  </verify>
  <done>All three venv interpreters exist and are executable, `pip check` is clean in each, `FROZEN.txt` is written, the chroot wiring sits before the cleanup block with renumbered comments, and the stale `install-arlowe-fs.sh` comment is corrected.</done>
</task>

<task type="auto">
  <name>Task 2: Produce the dashboard standalone bundle</name>
  <files>runtime/dashboard/next.config.ts, runtime/dashboard/package.json, pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh, pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh, .github/workflows/ci.yml, units/arlowe-dashboard.service</files>
  <action>
Set `output: "standalone"` in `runtime/dashboard/next.config.ts`. Without it Next emits no
`server.js` at all and the unit's ExecStart target can never exist.

Write `pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh`, run inside the chroot against
`/opt/arlowe/runtime/dashboard` (already populated by the rsync). Steps:

  1. The package manager is **pnpm**, not npm. `runtime/dashboard/` has `pnpm-lock.yaml` and
     `pnpm-workspace.yaml` and NO `package-lock.json`; `npm ci` will fail.

     PIN IT FIRST. `runtime/dashboard/package.json` has no `packageManager` field, and
     `.github/workflows/ci.yml` pins a floating `version: 10` at four call sites (lines ~99, 120,
     141, 164) while its own comment at line ~82 records that `node_modules` was authored by
     **10.15.0**. With the Python side fully pinned and `--frozen-lockfile` in force, the package
     manager version is the last build input left floating — and it is the one that resolves the
     lockfile. Add `"packageManager": "pnpm@10.15.0"` to `runtime/dashboard/package.json` (confirm
     10.15.0 against the lockfile's `lockfileVersion` and the CI comment before writing it; if the
     tree says something else, use what the tree says and note the discrepancy).

     Then have BOTH consumers derive from that one field rather than restating a version:
       - `.github/workflows/ci.yml`: drop `version: 10` from all four `pnpm/action-setup@v4` steps.
         `action-setup` reads `packageManager` from `package.json` when no version is given —
         confirm that against the action's documented behaviour for the pinned major version before
         relying on it, and if it does not, read the field with `jq` into a step output and pass
         that. Update the line-82 comment block to say where the pin now lives.
       - `build-dashboard.sh`: read `packageManager` out of
         `/opt/arlowe/runtime/dashboard/package.json` and activate exactly that version via
         `corepack` (or a pinned global install — state which, and why). Hard-fail if
         `pnpm --version` does not match the pinned value. Do not hardcode the version in the
         script; a second copy is a second thing to forget.
  2. `pnpm install --frozen-lockfile` with devDependencies available (`next build` needs typescript
     and the tailwind postcss plugin). Do not set `NODE_ENV=production` during install.
  3. `NEXT_TELEMETRY_DISABLED=1 pnpm build`.
  4. RELOCATE. Next standalone output does not land where the unit expects, and it is incomplete by
     design — this is the well-known standalone trap and skipping it yields a `server.js` that boots
     and serves an unstyled 404 for every static asset:
       - `.next/standalone/` contains `server.js` and a pruned `node_modules`
       - `.next/static/` must be copied to `.next/standalone/.next/static/`
       - `public/` must be copied to `.next/standalone/public/`
     Then move the assembled `.next/standalone/*` up so that `server.js` sits at
     `/opt/arlowe/runtime/dashboard/server.js`, which is what `units/arlowe-dashboard.service:18`
     names. Verify the final tree has `server.js`, `.next/static/`, and `public/` all under
     `/opt/arlowe/runtime/dashboard/`.
     If Next 16 places the standalone entry point at a nested path (it does this when it infers a
     workspace root above the project), set `outputFileTracingRoot` in `next.config.ts` to pin it,
     rather than hardcoding a guessed nesting depth in the shell script.
  5. CLEAN UP. Delete the build-time `node_modules` (the full install is several hundred MB; the
     pruned copy inside the standalone bundle is the one that ships), `.next/cache`, and the
     TypeScript/lint/test surfaces that have no business on a device: `tests/`, `playwright.config.ts`,
     `playwright-report/`, `test-results/`, `tsconfig.tsbuildinfo`, `eslint.config.mjs`.
     Report `du -sh` before and after to the build log.
  6. Set ownership root:arlowe and permissions matching the rest of `/opt/arlowe/runtime`.

NODE RUNTIME. Apply the decision from ADR-0008 (plan 07.1-01). Bookworm's `nodejs` is **18.20.4**;
`next@16.1.6` declares `engines.node >= 20.9.0`. Both the build in step 3 and the runtime in the unit
need the newer Node.

Source the tarball from `third_party/node/manifest.yml` (plan 07.1-01 wrote it, wired it into
`scripts/verify-third-party.sh`, added `third_party/node` to `00-run.sh`'s staging loop, and declared
`curl`/`ca-certificates`/`xz-utils` in `00-packages-nr` so the chroot can unpack it). Verify the
SHA-256 against the manifest in this script too before unpacking — fetch-time and build-time are
different moments and the gate that runs on the host does not protect the chroot. Unpack to the
prefix the ADR names.

THREE ASSERTIONS, not instructions. Every one of these must be a hard failure, because the failure
they prevent (a Node 20 on `PATH` at build time, a Node 18 at the unit's `ExecStart` path, and a
dashboard that never starts) leaves every other gate green:

  (i)   In this script: `"<prefix>/bin/node" --version` — the ABSOLUTE path you just unpacked, not
        `node` off `PATH` — must satisfy >= 20.9.0. Hard-fail otherwise.
  (ii)  Edit `units/arlowe-dashboard.service:18` so `ExecStart=` names that absolute path
        explicitly. This edit is UNCONDITIONAL, not "if it is not /usr/bin/node": the unit must name
        the interpreter this build produced, whatever ADR-0008 chose, including the case where the
        ADR diverted `/usr/bin/node` itself. Leaving the decision implicit is how the wrong Node
        survives.
  (iii) Plan 07.1-03's `verify_unit_runtime_versions` is the enforcement authority: it probes the
        interpreter the unit NAMES against the 20.9.0 floor in the built rootfs. Assertion (i) alone
        is not sufficient, because it says nothing about the ExecStart path; (iii) alone is not
        sufficient, because it runs late in the build. Both, deliberately. Confirm in Task 3 that
        (iii) sees the path (ii) wrote.

Wire the call into `00-run-chroot.sh` immediately after the `build-venvs.sh` call from Task 1, inside
the same renumbered step block and before the cleanup.
  </action>
  <verify>
Run the dashboard build in an arm64 container with network, against a copy of the runtime tree:

  docker run --rm --platform linux/arm64 -v "$PWD:/repo" debian:bookworm bash -c '
    set -e
    apt-get update -qq >/dev/null
    apt-get install -y -qq --no-install-recommends ca-certificates curl xz-utils jq >/dev/null
    M=/repo/third_party/node/manifest.yml
    NODE_URL="$(awk "/^url:/{print \$2}" "$M")"; NODE_SHA="$(awk "/^sha256:/{print \$2}" "$M")"
    NODE_PREFIX="$(awk "/^install_to:/{print \$2}" "$M")"
    curl -fsSL "$NODE_URL" -o /tmp/node.tar.xz
    echo "$NODE_SHA  /tmp/node.tar.xz" | sha256sum -c -
    mkdir -p "$NODE_PREFIX"
    tar -xJf /tmp/node.tar.xz -C "$NODE_PREFIX" --strip-components=1
    "$NODE_PREFIX/bin/node" --version
    export PATH="$NODE_PREFIX/bin:$PATH"
    mkdir -p /opt/arlowe/runtime
    cp -r /repo/runtime/dashboard /opt/arlowe/runtime/dashboard
    rm -rf /opt/arlowe/runtime/dashboard/node_modules /opt/arlowe/runtime/dashboard/.next
    bash /repo/pi-gen/stage-arlowe/01-runtime/files/build-dashboard.sh
    test -f /opt/arlowe/runtime/dashboard/server.js || { echo "NO server.js"; exit 1; }
    test -d /opt/arlowe/runtime/dashboard/.next/static || { echo "NO static"; exit 1; }
    test -d /opt/arlowe/runtime/dashboard/public || { echo "NO public"; exit 1; }
    test ! -d /opt/arlowe/runtime/dashboard/tests || { echo "tests shipped"; exit 1; }
    echo DASHBOARD_OK; du -sh /opt/arlowe/runtime/dashboard'

Then prove it actually serves, which `test -f` does not:
    cd /opt/arlowe/runtime/dashboard && PORT=3000 HOSTNAME=127.0.0.1 NODE_ENV=production \
      ARLOWE_CONFIG_PATH=/nonexistent node server.js &
    sleep 8
    curl -sS -o /dev/null -w "%{http_code}\n" http://127.0.0.1:3000/
Expected, in order: `/tmp/node.tar.xz: OK` from `sha256sum -c`, a `v20.x` line at or above
`v20.9.0` from the absolute-path `node --version`, then `DASHBOARD_OK`, a size figure, and `200`.
`ARLOWE_CONFIG_PATH=/nonexistent` is the unpaired factory state (CONFIG-03) — the dashboard must
serve in it, not crash.

Also assert the pin took, and paste all four lines into the SUMMARY:
    jq -r .packageManager runtime/dashboard/package.json          # expect pnpm@10.15.0
    grep -c "version: 10$" .github/workflows/ci.yml               # expect 0
    grep -n "ExecStart=" units/arlowe-dashboard.service           # expect the vendored node prefix
    docker run ... "$NODE_PREFIX/bin/pnpm --version"              # expect 10.15.0, matching the pin
  </verify>
  <done>`server.js` exists at the path the unit names, `.next/static` and `public` are alongside it, the build-time `node_modules` and test surfaces are gone, the tarball's SHA-256 verified, the absolute-path `node --version` satisfies the 20.9.0 floor, `units/arlowe-dashboard.service` names that absolute path, pnpm is pinned in `package.json` with CI and the chroot script both deriving from it, and the server returns 200 with no config overlay present.</done>
</task>

<task type="auto">
  <name>Task 3: Prove both SC1 gates now pass on a rootfs shaped like the fixed build</name>
  <files>pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh</files>
  <action>
Combine Task 1's and Task 2's container fixtures into one rootfs tree, add the units via
`units/install-units.sh`, and run BOTH of plan 07.1-03's gates against it — `verify_unit_execstart`
and `verify_unit_runtime_versions`, with `ARLOWE_VERSION_PROBE` unset so the version gate really
probes the binaries this plan installed. This is the integration check that the two plans agree:
07.1-03 asserts what 07.1-04 must produce, and nothing has proven they match until this runs. It is
also the only place the two halves of the Node fix meet — the `ExecStart=` path Task 2 wrote and the
floor the version gate enforces.

If either gate reports a FAIL, fix the producing side here; do not relax the gate. If either reports
a SKIP or an UNDECLARED you did not expect, investigate it — those are things the gate could not see,
which is precisely the category this phase is about.

Then re-read `00-run-chroot.sh` end to end and confirm the final step ordering is coherent: user ->
fs -> config -> units -> udev/polkit -> runtime rsync -> CLI symlinks -> venvs -> dashboard -> axcl
-> whisplay -> wm8960 -> fstab -> cleanup. Update the header comment block (lines 9-28) so it
describes the script as it now is. Leaving the header describing an older script is the same defect
class as the stale `install-arlowe-fs.sh` comment.
  </action>
  <verify>
  Run the combined fixture and paste BOTH gates' full output into the SUMMARY. Expected from
  `verify_unit_execstart`: zero FAIL, with every previously-failing path now PASS — the three venv
  interpreter paths (`/opt/arlowe/venvs/{voice,llm,stt}/bin/python`) across all seven `Exec*` stanzas,
  plus `/opt/arlowe/runtime/dashboard/server.js`. Expected from `verify_unit_runtime_versions`: zero
  FAIL, the dashboard's interpreter reported at a version >= 20.9.0 and the venv interpreters at
  >= 3.11.0, and `qwen-api`'s `run_api.sh` reported UNDECLARED rather than silently skipped.
  `bash -n pi-gen/stage-arlowe/01-runtime/00-run-chroot.sh` exits 0 and `shellcheck` is clean on all
  three modified shell files.
  </verify>
  <done>Both SC1 gates pass on a rootfs built by these scripts — including the version gate probing the interpreter `units/arlowe-dashboard.service` now names — and `00-run-chroot.sh`'s header describes its actual step order.</done>
</task>

</tasks>

<verification>
1. The three venvs build in an arm64 bookworm container and `pip check` is clean in each.
2. `server.js` exists at `/opt/arlowe/runtime/dashboard/server.js` and serves HTTP 200 with no config overlay present.
3. The Node at the absolute prefix from `third_party/node/manifest.yml` reports `>= 20.9.0`, its tarball SHA-256 verified, and `units/arlowe-dashboard.service` names that absolute path.
4. `verify_unit_execstart` AND `verify_unit_runtime_versions` both return zero FAIL against the combined fixture rootfs, with `ARLOWE_VERSION_PROBE` unset.
5. `shellcheck` clean on `build-venvs.sh`, `build-dashboard.sh`, `00-run-chroot.sh`.
6. The `du -sh` figures for `/opt/arlowe/venvs` and `/opt/arlowe/runtime/dashboard` are recorded, with an explicit note on the ADR-0004 card-sizing consequence.
</verification>

<success_criteria>
- `/opt/arlowe/venvs/{voice,llm,stt}/bin/python` exist, are executable by the `arlowe` group, and each venv passes `pip check`.
- `/opt/arlowe/runtime/dashboard/server.js` exists with `.next/static` and `public` beside it, and no build-time `node_modules` or test surfaces ship.
- Both build steps run inside `00-run-chroot.sh` after their inputs exist and before the staged tree is deleted, with step comments renumbered.
- pnpm is pinned once in `runtime/dashboard/package.json`; neither `ci.yml` nor `build-dashboard.sh` restates a version.
- `install-arlowe-fs.sh` no longer claims Phase 6 will populate the venvs.
</success_criteria>

<output>
Create `.planning/phases/07.1-runtime-substrate-repair/07.1-04-SUMMARY.md`.
Record: the mechanism used to get the requirement files into the chroot; both `du -sh` figures and
the card-sizing consequence; the Node install prefix and version, the SHA verification, and the exact
`ExecStart=` line the dashboard unit now carries; the pnpm version pinned and how CI derives it; and
both gates' output from Task 3.
</output>
