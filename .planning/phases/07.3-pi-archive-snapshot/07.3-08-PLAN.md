---
phase: 07.3-pi-archive-snapshot
plan: 08
type: execute
wave: 13
depends_on: ["07.3-07b"]
files_modified:
  - docs/operations/phase-07.2-inputs.reference
autonomous: false

must_haves:
  truths:
    - "A pinned full build on a destroyed work dir completes and emits an image, with the Pi gate logging '0 off-pin' and the completeness check passing."
    - "Positive control: the build log shows apt activity against the local flat repo (count > 0), so the zero-Pi-fetch assertion cannot pass vacuously."
    - "No archive.raspberrypi.com fetch appears anywhere in the build log, including the kernel install."
    - "The shipped slot-A and slot-B rootfs carry the stock raspi.list and no flat repo."
    - "Build A's input manifest differs from the pre-phase reference ONLY in pin rows (3 rpt rows out, the pi-archive rows in); zero pkg rows change."
    - "The re-recorded reference is reviewed and merged by the owner before 07.3-09 starts."
  artifacts:
    - path: "docs/operations/phase-07.2-inputs.reference"
      provides: "Reference re-recorded once from build A (pin rows only)"
      contains: "third_party/pi-archive/"
  key_links:
    - from: "docs/operations/phase-07.2-inputs.reference"
      to: "scripts/record-build-inputs.sh"
      via: "re-recorded by ARLOWE_INPUTS_ACCEPT=1 during build A"
      pattern: "pin\tthird_party/pi-archive/"
---

<objective>
Run build A: the first pinned full build. Prove that the flat repo works inside a real pi-gen run and that pinning reproduces the known-good package set. Re-record the reference's pin rows once, and hand the reference PR to the owner to merge.

Purpose: plans 01a–07b are verified by fixtures, containers and CI resolutions. This is the first time the mechanism runs inside pi-gen. It also establishes the baseline that 07.3-09's build B must match without a re-record (SC4). Build B is a separate plan because it cannot start until this reference is merged AND the archive has published, which takes at least a day.

**Honest PR size: ~95 lines.** The reference diff is 3 rpt pin rows removed + 92 pi-archive pin rows added = 95. It is generated, and it still counts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3-pi-archive-snapshot/07.3-RESEARCH.md
@.planning/phases/07.3-pi-archive-snapshot/07.3-07a-SUMMARY.md
@.planning/phases/07.2-build-input-pinning/07.2-04-PLAN.md
@docs/operations/phase-07.3-pi-archive-pinning.md
</context>

<execution_notes>
Carried over from 07.2-04 and MEMORY:
- **Destroy the work dir first** (`sudo rm -rf build/pi-gen-work`), then confirm it is gone. A stale rootfs makes every result a statement about the wrong rootfs (Pitfall 4).
- **The build host has no GitHub auth.** rsync the tree over from the development machine, `.git` included. Never `git pull` there. Sync artifacts back the same way.
- **`worktree_clean` is in the diff gate's scope.** Build from a clean worktree.
- **Refer to the build host by role** in everything committed, including this plan's SUMMARY. The sanitize gate fails on its hostname.
- **Poll with `pgrep -f 'build-image[.]sh'`**, never with a bare pattern.
- **Never pipe `sudo -S` into `tee`.** Use one `sudo -S bash -c`.
- **Mount the image read-only** for inspection.
- `AXCL_DEB` must be set and `dpkg-dev` installed. Check both before a 30+ minute run.
- **ACCEPT is set for this build only, and it is safe only because of Step 5's zero-pkg-row assertion.** No other build in this phase may set it (02's standing rule).
- If ANY assertion fails, stop and record it verbatim. Failures here go to gap-closure plans, not ad-hoc fixes.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Build A with evidence and a pin-row-only re-record</name>
  <files>docs/operations/phase-07.2-inputs.reference</files>
  <action>
**Step 1: preconditions.** main must contain 01a–07b.
- Sync the tree to the build host.
- Worktree clean, `dpkg-scanpackages` present, `AXCL_DEB` set.
- Overlay record count equals the tracked overlay files minus 1.
- `ARLOWE_KERNEL_FETCH=1 ARLOWE_PI_ARCHIVE_FETCH=1 scripts/verify-third-party.sh` passes.

**Step 2: fingerprint the archive.** Run `bash tests/phase-07.3/probe-pi-archive.sh --state | tee build/pi-archive-state-A.txt`. Record it in the SUMMARY with a UTC timestamp. `build/` is gitignored, so also copy `build/pi-archive-state-A.txt` back to the development machine's `build/` when you sync artifacts back; 07.3-09 Task 1 diffs against it and may run on either machine.

**Step 3: build.** `sudo rm -rf build/pi-gen-work` (confirm it is gone), then `ARLOWE_INPUTS_ACCEPT=1 scripts/build-image.sh 2>&1 | tee build/logs/phase-07.3-build-A.log`.

**Step 4: evidence.** Quote each item verbatim in the SUMMARY:
0. debootstrap ran; it was not skipped.
1. The **positive control**: `grep -cE '(Get|Hit|Ign|Err):[0-9]+ file:/var/local/arlowe-pi-archive' LOG` is **> 0**. If it is 0, apt's output isn't in the log, and items 3 and 4 would be vacuous. STOP and find where pi-gen logged apt output (e.g. `build/pi-gen-work/build.log`), then re-run the `<verify>` block with `LOG=<that file>` set; the block defaults `LOG` only when it is unset.
2. `Pi archive resolution pinned: N flat-repo list files, 0 off-pin`, and the check's `0 unattributed` line.
3. `grep -cE '(Get|Hit|Ign|Err):[0-9]+ https?://archive\.raspberrypi\.com' LOG` is **0**. Also quote the `02-firmware` section's `Need to get` line. The kernel should install with no Pi-side download; this confirms or refutes ADR-0010's expected side effect.
4. The flat repo's Pitfall 9 `Err:…Packages.xz` noise is present and harmless.
5. `Pi flat repo removed` and `Build complete`; the `.img` exists.
6. Slot A (p2) and slot B (p3), each mounted **read-only**:
   - `etc/apt/sources.list.d/raspi.list` is identical (`cmp`) to the upstream file with RELEASE substituted;
   - `var/local/arlowe-pi-archive` is absent;
   - `arlowe-pi-archive.list` is absent.
7. `grep -c 'export-image' LOG` and `ls pi-gen/deploy/ build/pi-gen-work/export-image 2>&1`. This is the SKIP_IMAGES evidence for the follow-up issue.

**Step 5: the re-record must touch pin rows only.**
- `git diff docs/operations/phase-07.2-inputs.reference | grep -cE '^[-+]pkg'` must be **0**. Anything else: STOP, don't commit, and record the rows.
- The `-pin` rows naming rpt-packages must number 3.
- The `+pin` rows naming `third_party/pi-archive/` must equal the manifest's entry count.

Sync the reference back and commit `chore(07.3): record the Pi-archive pins in the inputs reference`. The orchestrator opens the PR. Record the build-A commit in the SUMMARY.
  </action>
  <verify>
    # runs-on: build host. PRE:
    ls build/pi-gen-work 2>&1                                                    # expect: no such file
    git status --porcelain --untracked-files=no | wc -l                         # expect: 0
    command -v dpkg-scanpackages && test -f "${AXCL_DEB:?}"
    # POST:
    LOG="${LOG:-build/logs/phase-07.3-build-A.log}"   # override if apt output lives in pi-gen's own build.log (Step 4 item 1)
    test "$(grep -cE '(Get|Hit|Ign|Err):[0-9]+ file:/var/local/arlowe-pi-archive' "$LOG")" -gt 0 || { echo "POSITIVE CONTROL FAILED: apt output not in log; zero-fetch count would be vacuous"; exit 1; }
    test "$(grep -cE '(Get|Hit|Ign|Err):[0-9]+ https?://archive\.raspberrypi\.com' "$LOG")" -eq 0 || { echo "Pi-archive fetch in build"; exit 1; }
    grep -c 'Pi archive resolution pinned: [0-9]* flat-repo list files, 0 off-pin' "$LOG"   # expect: 1
    grep -c 'Build complete' "$LOG"                                                         # expect: 1
    test "$(git diff docs/operations/phase-07.2-inputs.reference | grep -cE '^[-+]pkg')" -eq 0 || { echo "pkg rows moved"; exit 1; }
  </verify>
  <done>Build A emitted an image. The positive control is > 0, Pi fetches are 0, the gate logged 0 off-pin and the check passed. Both slots ship the stock raspi.list. The reference changed only in pin rows and is committed.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Owner reviews and merges the reference PR</name>
  <what-built>Build A: the first pinned build of the Pi archive side, plus a re-recorded inputs reference whose only change is pin rows.</what-built>
  <how-to-verify>
1. Read the SUMMARY's positive-control count (must be > 0) and the zero Pi-fetch count.
2. Confirm that the reference PR's diff contains only `pin` rows: 3 `-` rows for rpt-packages and one `+` row per pi-archive entry, with no `pkg` rows.
3. Merge the PR. 07.3-09 must build from a commit that contains this reference.
4. Note the build-A UTC time in the SUMMARY. 07.3-09 cannot start sooner than 24 hours after it.
  </how-to-verify>
  <resume-signal>Type "merged" once the reference PR is on main, or name what is wrong.</resume-signal>
</task>

</tasks>

<verification>
- Build A: positive control > 0, 0 Pi fetches, 0 off-pin, check passes, image emitted, stock raspi.list on both slots, reference diff limited to pin rows, PR merged.
</verification>

<success_criteria>
The mechanism is proven inside a real pi-gen run, and the SC4 baseline is on main.
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-pi-archive-snapshot/07.3-08-SUMMARY.md` with every Step 4 item verbatim, the state-A fingerprint, the build-A UTC time and the reference commit.
</output>
