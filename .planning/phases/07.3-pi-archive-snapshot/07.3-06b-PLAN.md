---
phase: 07.3-pi-archive-snapshot
plan: 06b
type: execute
wave: 10
depends_on: ["07.3-06a"]
files_modified:
  - scripts/verify-third-party.sh
  - third_party/rpt-packages/manifest.yml
  - pi-gen/stage-arlowe/00-packages/00-packages-nr
autonomous: true

must_haves:
  truths:
    - "third_party/rpt-packages/ and the old check 8 no longer exist; raspi-firmware, python3-lgpio and python3-rpi-lgpio are pinned only in third_party/pi-archive/manifest.yml."
    - "verify-third-party.sh's header and usage list checks 1-8, with 8 being the Pi archive check."
    - "Nothing in the repo references rpt-packages except the stale pin rows in the inputs reference, which 07.3-08 re-records."
  artifacts:
    - path: "scripts/verify-third-party.sh"
      provides: "Check 8 = Pi archive (renumbered from 9)"
      contains: "pi-archive-fetch.py"
  key_links:
    - from: "scripts/verify-third-party.sh"
      to: "third_party/pi-archive/manifest.yml"
      via: "check 8"
      pattern: "third_party/pi-archive/manifest.yml"
---

<objective>
Retire `third_party/rpt-packages/` and the old check 8, now that both of their consumers are gone: 03 replaced the verification and 06a removed resolve-twice's exclusion.

Purpose: completes SC3's "folded" half for the three rpt pins. Two copies of a pin is how a pin half-applies.

**Honest PR size: ~200 lines.**
- check 8 stanza deleted: 100
- header and usage items 8/9 renumbered: 6
- rpt manifest deleted: 76
- 00-packages-nr comment: 4
- prose-pointer headroom: 14

100 + 6 + 76 + 4 + 14 = 200. Deletions count toward the cap. That is why this is its own PR.

**Test-first note.** This is a deletion. The "test" is a set of assertions that the retired pieces are gone and nothing depends on them. Before the change, `test ! -e third_party/rpt-packages` fails and the reference grep finds hits beyond the inputs reference. After it, both pass. The existing self-tests (`test-pi-archive-committed.sh`, `test-pi-archive-fetch.sh`) must still pass unchanged.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07.3-pi-archive-snapshot/07.3-03-SUMMARY.md
@.planning/phases/07.3-pi-archive-snapshot/07.3-06a-SUMMARY.md
@scripts/verify-third-party.sh
@third_party/rpt-packages/manifest.yml
</context>

<execution_notes>
- Run `git diff --shortstat main -- . ':(exclude).planning/**'` after Task 1.
- Don't hand-edit `docs/operations/phase-07.2-inputs.reference`. Its `pin third_party/rpt-packages/…` rows are declared-from-repo rows that 07.3-08's first build re-records.
- Carry the rpt manifest's prose forward: raspi-firmware as the bootloader, the ten-hour drift, the resolve-twice exit-100 story, and digests confirmed by hashing the served bytes. List pointers in the SUMMARY so 07.3-07b includes them in the runbook.
</execution_notes>

<tasks>

<task type="auto">
  <name>Task 1: Record the RED assertions, then retire rpt-packages and check 8</name>
  <files>scripts/verify-third-party.sh, third_party/rpt-packages/manifest.yml, pi-gen/stage-arlowe/00-packages/00-packages-nr</files>
  <action>
1. RED: run the three assertions in `<verify>` before editing, and record that they fail.
2. `git rm third_party/rpt-packages/manifest.yml`.
3. `scripts/verify-third-party.sh`:
   - Delete the `# Check 8: pinned Raspberry Pi archive packages` stanza, through its closing `fi`.
   - Delete item 8 from the header list and from `usage()`.
   - Renumber check 9 to 8 in the header, `usage()` and the stanza comment.
   - Drop the stanza comment's "check 8 is retired once nothing reads it" clause.
4. `pi-gen/stage-arlowe/00-packages/00-packages-nr`: the python3-rpi-lgpio comment's last sentence now says the package is pinned by sha256 in `third_party/pi-archive/manifest.yml` and installed from the build's digest-verified flat repo.
  </action>
  <verify>
    test ! -e third_party/rpt-packages && echo "retired"                                        # RED before, OK after
    grep -rn 'rpt-packages\|ARLOWE_RPT\|RPT_ONLY' --exclude-dir=.planning --exclude-dir=node_modules . | grep -v 'phase-07.2-inputs.reference'; echo "rc=$? (expect 1 after)"
    [ "$(grep -cE '^#  +[0-9]+\. ' scripts/verify-third-party.sh)" -eq 8 ] && grep -qE '^#  +8\. .*pi-archive' scripts/verify-third-party.sh && echo "header 1-8 OK"
    shellcheck scripts/verify-third-party.sh
    bash tests/phase-07.3/test-pi-archive-committed.sh && bash tests/phase-07.3/test-pi-archive-fetch.sh
    git diff --shortstat main -- . ':(exclude).planning/**'                                     # expect: ~200
  </verify>
  <done>The three rpt pins live in one place, the header lists checks 1–8, the existing self-tests pass, and the carried-over prose is listed in the SUMMARY.</done>
</task>

</tasks>

<verification>
- All three assertions went from failing to passing, and the unchanged self-tests still pass.
</verification>

<success_criteria>
SC3's rpt fold is complete, with no duplicated pin left behind.
</success_criteria>

<output>
After completion, create `.planning/phases/07.3-pi-archive-snapshot/07.3-06b-SUMMARY.md` with the prose pointers for 07b.
</output>
