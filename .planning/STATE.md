# Project State

## Project Reference

See: .planning/PROJECT.md (updated 2026-04-30)

**Core value:** A factory-fresh Pi 5 + AX accelerator + Whisplay can flash this image, boot, pair to an owner, and run wake -> STT -> LLM -> TTS -> face entirely on-device, with no founder identity present anywhere in the image.
**Current focus:** Phase 6 hardware checkpoint IN PROGRESS (round-3 diagnostic done 2026-09-08; F7 #18 fixed in source, awaiting a clean rebuild + re-flash on arlowe-1). Phase 7 is unblocked — the owner_state/identity blocker (F7 #17) was a false alarm. Decision: run the deferred on-Pi flash+boot checkpoint NOW, before starting Phase 7, while owner is physically at the hardware — Phase 6 is the first flashable artifact and Phase 7 (device identity/PKI) is the first phase whose deliverables physically live on the Phase 6 substrate (`/var/lib/arlowe/identity/`, CPU-serial device-id), so the substrate must be validated before building PKI on it. This is also the FIRST-EVER build of the image (no CI image build has ever run). Build host: arlowe-1 (native pi-gen). After the checkpoint: Phase 7 — ADR pending (managed-PKI service selection; owner leaning AWS IoT); recommend `/gsd:discuss-phase 7` before planning.

## Current Position

Phase: 7 of 12 (Device identity and PKI) — IN PROGRESS. Phase 6 COMPLETE in code; its hardware checkpoint is separately blocked (SC3, F7 #25/#28).
Plan: 2 of 11 in Phase 7 (07-01, 07-02 complete)
Status: Phase 6 closed 2026-06-14. Flashable 5-partition A/B image pipeline: 06-01 reconciled shared-model sizing (16 GB viable / 32 GB recommended) + wrote ADR-0004 (partition sizing), ADR-0005 (tryboot root= A/B selector), ADR-0006 (Whisper model selection); 06-02 SHA-pinned model + WhisPlay manifest/verify gate (#112); 06-03 pi-gen stage-arlowe chroot provisioning, model-free rootfs (#113); 06-04 build-image.sh measure-then-set 5-partition A/B + models grow-to-fill (#114); 06-05 tryboot root= selector + arlowe-ab flip CLI + slot-B recovery stub (#115); 06-06 arm64 image-build CI + shellcheck gate + flash-sd.sh + dev-deploy.sh + runbook (#116). On-Pi flash+boot (SC1-SC5) deferred to a hardware checkpoint per Phase 1/3/4/5 precedent (runbook: docs/operations/phase-6-build-flash-deploy.md). Next: Phase 7 (device identity and PKI) — depends on Phase 6 (done) + Phase 4 (done); ADR pending for managed-PKI service selection.
Last activity: 2026-07-08 -- RESUMED the Phase 6 hardware checkpoint. Card decided: 64 GB card -> CARD_SIZE_GB=32 (blocker cleared). WhisPlay gate needed WhisPlay.py + Apache LICENSE co-located; staged ~/whisplay-staging/{WhisPlay.py (proven dev-unit copy),LICENSE (from fresh PiSugar/Whisplay clone)} on arlowe-1 -> verify-third-party GREEN (only expected model-TODO + WM8960 WARNs). FIRST-EVER BUILD: launch #1 (21:28) FAILED at pi-gen stage0/00-configure-apt with NO_PUBKEY on deb.debian.org repos -- ROOT CAUSE: unpinned pi-gen master targets trixie, its debian.sources uses Signed-By=.../debian-archive-keyring.PGP but bookworm ships .GPG -> apt can't verify. FIX (local patch on arlowe-1 pi-gen tree, sed .pgp->.gpg in stage0/00-configure-apt/files/debian.sources) + filed todo F6 (pin pi-gen). Relaunch #2 (22:06) FAILED at stage2/01-sys-tweaks/00-packages: "Unable to locate package rpi-swap/rpi-loop-utils/rpi-usb-gadget" -- SAME root cause: trixie-only packages in trixie-master pi-gen. STOPPED symptom-patching. FIX (2026-07-09 01:20): re-pinned pi-gen to tag 2026-06-18-raspios-bookworm-arm64 (d7a31c6) -- clones upstream at that ref, restores arlowe overlay (config + stage-arlowe) from git. Confirmed bookworm pi-gen has NO signed-by keyring bug (plain sources.list) and NO rpi-* trixie pkgs. Build #3 (01:20) SUCCEEDED through all upstream pi-gen (stage0/1/2) -- bookworm pin fully worked -- then FAILED entering arlowe's OWN stage-arlowe/01-runtime/00-run-chroot.sh ("Unable to chroot"). ROOT CAUSE: stage-arlowe is arlowe's never-executed Phase-6 code and has multiple latent bugs (no build ever reached it before). Found + FIXED IN REPO (Mac, rsynced to arlowe-1): (1) missing stage-arlowe/prerun.sh -> rootfs never populated [blocker]; (2) 01-runtime/00-run.sh stages third_party/whisplay-driver from REPO which lacks WhisPlay.py/LICENSE (they were only in ARLOWE_WHISPLAY_SRC=~/whisplay-staging) -> driver missing from image; worked around by also copying WhisPlay.py+LICENSE into repo third_party/whisplay-driver/; (3) 03-firstboot/files/ (service + arlowe-grow-models.sh) never staged into chroot -- pi-gen does NOT auto-copy files/ -- grow-models had no fallback -> SC2 grow-to-fill would silently not install; fixed 03-firstboot/00-run.sh to stage files/ into chroot /files/. Also: WhisPlay.py is NOT gitignored despite INSTALL.md claim (commit-hygiene gap). MATERIAL: Phase 6 "6/6 complete in code" was NEVER end-to-end validated -- its final image stage never ran. Builds #4/#5 surfaced TWO MORE structural stage-arlowe bugs: (4) all host *-run.sh scripts committed non-exec (100644) -> pi-gen skips host scripts lacking +x (-x test) while running chroot scripts (-f) -> host staging never ran [fixed: chmod +x all + prerun, in repo]; (5) 01-runtime/00-run.sh stages repo to /tmp/arlowe-build/repo but pi-gen on_chroot mounts tmpfs OVER /tmp before every chroot step -> repo masked -> "staged repo not found" [UNFIXED - needs staging path moved off /tmp]. **CHECKPOINT PAUSED ~02:00 EDT 2026-07-09 at build #5.** FIVE structural bugs found, ALL in stage-arlowe plumbing, NONE yet the actual provisioning logic (repo never reached chroot). Stopped autonomous 2am hot-patching per commitment. RECOMMEND: reopen Phase 6, give stage-arlowe a real DEV/QA pass (build green in dev loop, commit each fix), then re-attempt checkpoint. All findings + fix specs in todo F7. Build inputs (models cache, whisplay-staging, axcl, bookworm-pinned pi-gen, CARD_SIZE_GB=32) all still staged on arlowe-1 for resume.

**UPDATE 2026-07-09 ~08:27 — FIRST IMAGE BUILT (owner approved driving to green).** After 10 fixes (5 plumbing + axcl deb #6-9 + build-image measure #10), a CLEAN FULL BUILD (no SKIP, stage0->stage-arlowe->partition in one pass, ~14 min) produced build/arlowe.img on arlowe-1. Correct 5-partition A/B layout (boot 537MB / system_a 2147MB / system_b 2147MB / owner_state 3221MB / models 26.3GB), sanitize gate passed, axcl driver built for image kernel 6.12.93+rpt-rpi-2712 (postinst runtime-load tolerated). Image SHA256 fb1ed0488261de0cc86f20ef0525063bba8126985c3fe86bbf5817f2369461fd (32 GiB file, 9.9G data). Fixes COMMITTED on feat/110: a43eb96 (stage-arlowe), c31936c (build-image measure), 0fcada2 (planning + gitignore). Caveats (F7): axclhost half-configured in dpkg; NPU runtime unvalidated (deferred, no accelerator); driver for shipped kernel only. **NEXT = physical checkpoint steps 5-7 (owner hands):** move 64GB card (Mac /dev/disk2) -> USB reader on arlowe-1 -> scripts/flash-sd.sh -> power-down/swap into arlowe-1 slot -> boot + USB-TTL serial @115200 for SC1/SC2/SC3. Phase 6 stays NOT-"passed" until the on-Pi SCs run green. NOTE this pin is LOCAL on arlowe-1 only -- repo build-image.sh still unpinned (F6 still open for the repo fix). NOTE watcher-bug: first watcher used bare pgrep -f "build-image.sh" which self-matched its own cmdline -> falsely reported RUNNING for 3h while build had died in 90s; corrected watcher uses regex "build-image[.]sh". Log ~/arlowe-build.log, CARD_SIZE_GB=32. Remaining checklist steps 5-7 (flash spare card via USB reader on arlowe-1 + physical serial SC run + record) need owner hands. Card currently in Mac (/dev/disk2); USB SD reader attached to arlowe-1 for the flash step. NOTE: WhisPlay provenance gap logged to F2 (dev copy 344 lines DIFFERS from upstream HEAD 30df9903 662 lines; resolve before any distributable image). NOTE: arlowe-1 already at branch HEAD bf5a20e (STATE's old "stale at 9206c63" was wrong) -- no rsync needed. See Session Continuity for resume checklist + the main-branch discrepancy below.

**UPDATE 2026-07-18 — CERTIFICATION BUILD + FROM-SCRATCH FLASH+BOOT (paused mid-diagnosis).** Resumed fix-forward per owner. Committed the last checkpoint fixes on feat/110: `2352231` seed owner_state p4 with the /var/lib/arlowe skeleton (`_pimg_seed_owner_state`, mirrors `_pimg_seed_models`); `5eb8239` floor system slot at ADR-0004 3072 MiB (measured wins only when larger); `f000915` generate .bmap in build-image.sh for fast flashing; `50dcb2b` F7 status correction. Installed `bmap-tools` on arlowe-1 (owner-approved). Synced fixes to arlowe-1 (targeted rsync of stage-arlowe + scripts, NOT --delete whole tree — upstream pi-gen checkout + vendored third_party live only on arlowe-1). CLEAN FROM-SCRATCH BUILD (wiped 15G build/pi-gen-work + old img; ~19.6 min) -> build/arlowe.img (9.9G data / 32 GiB). Loop-mount validated ON THE IMAGE: fstab has real PARTUUIDs (no BOOTDEV/ROOTDEV), owner_state p4 seeded with identity/ (995:992=arlowe:arlowe, 0700), slot 3.0G/1.1G-free (was 55M/97%). Flashed /dev/sda via bmaptool (9.9 GiB / 30.9% mapped). **FLASH GOTCHA:** my rw loop-mounts (for the on-image validation) dirtied p2/p4 ext4 superblocks AFTER the .bmap was generated -> first flash aborted on bmaptool checksum mismatch at p2 superblock (block 131328). FIX: regenerate .bmap AFTER any mount, then flash (LESSON: mount -o ro for inspection). Second flash clean (20m57s @ 8.1 MiB/s slow card). ON-HARDWARE (owner swapped card into arlowe-1 SD slot, HDMI+kbd, login pi/raspberry): **SC1 PASS from-scratch** — boots clean to login shell (kernel 6.12.93+rpt-rpi-2712), /etc/arlowe/config.yml ABSENT = ready-to-pair, NO emergency mode (fstab fix holds on a from-scratch image, not just the hand-patched card). **TWO ON-HARDWARE FAILURES (F7 #16, #17):** (16) `arlowe-firstboot` unit FAILED (active=failed) -> models NOT grown (still 22.5G on a 58.2G card); failure is EARLY in arlowe-grow-models (models still mounted => died at the `sudo parted -s` last-partition safety check L95 or `growpart` L113, before the L132 unmount). (17) **owner_state /var/lib/arlowe is EMPTY at runtime — `identity/` MISSING — Phase-7 STILL blocked** despite the build-time seed that I verified present on the image's p4. Unexplained: seed was on the image, flashed, but not visible at runtime (owner_state mounted-but-empty, or not mounted). **PAUSED before capturing the diagnostic.** Test card LEFT IN arlowe-1 (powered off); owner's own 238G card is OUT (aside). RESUME: power arlowe-1 on (boots test card; firstboot sentinel never written so grow re-runs+re-fails => journal still available), then run `sudo journalctl -u arlowe-firstboot -b --no-pager | tail -20; findmnt /var/lib/arlowe; sudo ls -la /var/lib/arlowe` and photo. A diagnostic SC-check script is on the card boot FAT at /boot/firmware/sc.sh (`bash /boot/firmware/sc.sh`). Built image preserved at build/arlowe.img on arlowe-1's own (removed) card. NO SSH to arlowe-1 while test card is in (no network; wifi rfkill-blocked, no ethernet). SC3 (A/B recovery) untested. All findings in F7.

**UPDATE 2026-09-08 — ROUND 3 DIAGNOSTIC: one failure was structural and much bigger, the other was never a bug.** Captured what the 2026-07-18 session paused before capturing (journal + mount table off the test card, HDMI+kbd on arlowe-1). **F7 #17 CLOSED — NOT A BUG. Phase 7 is NOT blocked.** `findmnt /var/lib/arlowe` -> `/dev/mmcblk0p4 ext4 rw,noatime`; `sudo ls -la` shows the full seeded skeleton (identity/, conversations/, dashboard/, logs/, state/, wake-word/, all arlowe:arlowe). The build-time seed `2352231` is verified end-to-end image->flash->runtime. The "empty" reading was a non-sudo `ls` as `pi` against a 0750 arlowe:arlowe dir. That inverted claim sat here as a Phase 7 blocker for seven weeks. **F7 #16 ROOT-CAUSED as new finding #18 — and it is far wider than growpart.** Journal: `arlowe-grow-models: line 113: growpart: command not found` (exit 127). The L95 `parted` hypothesis was WRONG — the safety check passed and execution continued to L113; "line 565" was the syslog PID, not a line number. Real cause: **`pi-gen/stage-arlowe/00-packages-nr` sat at the STAGE ROOT, which pi-gen never reads.** Verified against upstream pi-gen at the pinned tag `2026-06-18-raspios-bookworm-arm64`: `run_stage()` runs only `prerun.sh` at the stage root and iterates DIRECTORIES only for sub-stages; `NN-packages-nr` is read from inside `${SUB_STAGE_DIR}`. Silently ignored — no warning, no build failure. So `0a6015c` (the #13 "fix" adding cloud-guest-utils+gdisk) edited a file nothing reads, and **the declared package set has been absent from every image ever built.** On-hardware `dpkg -l`: ABSENT cloud-guest-utils/nodejs/npm/ripgrep/python3-rpi.gpio; PRESENT alsa-utils/gdisk/network-manager/python3-spidev (all RPi OS Lite base). Proof it never ran is internal: python3-spidev `ii` and python3-rpi.gpio `un` are adjacent in the same list, and pi-gen installs a list with one all-or-nothing `apt-get install`. **Blast radius:** no Node runtime for arlowe-dashboard (Phase 4/5 deliverable), no python3-rpi.gpio for the WhisPlay driver, no ripgrep — Phases 4/5 "passed-with-notes" rest on a substrate that cannot run their services. **FIXED on feat/110:** moved the list to `pi-gen/stage-arlowe/00-packages/00-packages-nr` (sorts before 01-runtime) + documented the placement constraint; deleted the dead stage-root `00-run.sh` whose comment asserted the false claim that caused this (same wrong model as F7 #1); **added a post-pi-gen guard in build-image.sh** that parses the declared list and asserts each package is `install ok installed` in the rootfs dpkg status, failing the build otherwise (matcher unit-tested against the four real on-hardware results). The guard is the durable fix — this failure class is a silent build-time no-op surfacing as a boot failure weeks later. **NEXT:** clean rebuild on arlowe-1 with the corrected sub-stage (needs the card swap — arlowe-1 has no network while the test card is in), re-flash, re-run SC2 grow + SC3. Test card still in arlowe-1; owner's 238G card still out.

**UPDATE 2026-09-08 (later) — F7 #18 FIX BUILT AND FLASHED.** arlowe-1 was reachable all along at 192.168.1.231/wlan0 (eth0 DOWN); it just took ~9 min for wifi association + mDNS after the card swap. An earlier "absent from the LAN" call from a ping/ARP sweep was wrong — a slow-joining host is indistinguishable from a missing one under that test. Synced to arlowe-1: `pi-gen/stage-arlowe/` with `--delete` (stale stage-root `00-packages-nr` + `00-run.sh` removed, `00-packages/00-packages-nr` in place) and `scripts/` (build guard). Wiped `build/pi-gen-work` (14G) + old `arlowe.img` for a genuine from-scratch build. CLEAN BUILD in 24 min. **The new guard reported `All 13 declared packages present in rootfs.` — first image ever where that is true.** Verified in the built rootfs: `usr/bin/growpart`, `usr/sbin/sgdisk`, `usr/bin/node`, `usr/bin/npm`, `usr/bin/rg`, `python3-rpi.gpio` (RPi module) all PRESENT. Layout correct (boot 537MB / system_a 3221MB / system_b 3221MB / owner_state 3221MB / models 24.2GB); `.bmap` generated post-build with NO rw loop-mounts after, so bmaptool accepted it first try (the July desync lesson held). FLASHED to the test card at /dev/sda (58.2G, removable, USB reader on arlowe-1; system disk mmcblk0 untouched) via bmaptool: 10.4 GiB of 32 GiB mapped, 22m10s @ 8.0 MiB/s, clean. **AWAITING OWNER: physical swap** — pull card from USB reader, power arlowe-1 down, swap its 238G card out, test card into the SD slot, boot, login pi/raspberry, then `systemctl status arlowe-firstboot --no-pager | head -5; df -h /opt/arlowe/models`. PASS = firstboot `active (exited)` and models ~48G (was 22.5G). Then SC3 (A/B recovery) is the last untested criterion. EXPECT: `arlowe-dashboard.service` now fails on the missing `server.js` instead of the missing interpreter — that is F7 #20, recorded, not a regression.

**UPDATE 2026-09-08 (SC2 PASS) — F7 #16 CLOSED ON HARDWARE.** Booted the rebuilt image on arlowe-1. `arlowe-firstboot`: ExecStartPre `arlowe-grow-models` and ExecStart `boot-check --first-boot` BOTH `status=0/SUCCESS`; unit state `inactive (dead)`, which is correct for `Type=oneshot`+`RemainAfterExit=no` (NOT a failure -- prior sessions should not misread this). `df -h /opt/arlowe/models` -> `/dev/mmcblk0p5 48G 6.2G 40G 14%` — grew 22.5G -> 48G filling the 58.2G card. **SC2 grow PASSES for the first time**, confirming the #18 chain end-to-end (package list in a sub-stage -> growpart installed -> grow succeeds). Both round-2 failures are now resolved: #16 fixed, #17 was never a bug. **Remaining for the checkpoint: SC3 only** (A/B tryboot flip + slot-B recovery + default reset to A), observable on HDMI now that the display works — the July note calling serial mandatory no longer applies. Non-hardware items open: #12 locked-root recovery console (design decision, ties to F8), #20 dashboard has no build step.

**UPDATE 2026-09-08 (SC3 PASS — CHECKPOINT SCs ALL GREEN).** Found and fixed F7 #21 first: `arlowe-ab` was "command not found" because `install-arlowe-cli.sh` links `arlowe-${cli} -> ${TARGET_DIR}/${cli}` from a hardcoded `CLIS=(... ab)`, but the repo file was `runtime/cli/arlowe-ab` — the only CLI carrying the prefix in its own filename — so the symlink pointed at a nonexistent `cli/ab`. `ln -sf` creates dangling links without error, so the installer reported "installed 9 symlinks" and exited 0. **SC3 was untestable, not untested — no amount of serial access in July would have run it.** Fixed in source (`b39339c`): renamed the file to match the convention every sibling follows, and made the installer fail loudly on a missing target. Hand-patched the running card to unblock SC3 tonight rather than burn 46 min on rebuild+reflash. **SC3 PASS:** `arlowe-ab switch B` -> reboot into slot B -> `arlowe-recovery.service` ran (console output on HDMI) -> reset default to slot A -> self-rebooted into A, unattended. Verified: `Persistent default: slot A` AND `findmnt / -> /dev/mmcblk0p2` (stored default and running slot agree). Pre-flight that de-risked it: PARTUUID map complete (PARTUUID_B present) and slot B's `multi-user.target.wants/` genuinely contained `arlowe-recovery.service` — first correctly-wired unit-enablement path found in Phase 6. **CAVEAT: SC3 ran on a hand-patched card**; the #21 source fix is NOT yet validated from a clean build (same posture as the July fstab patch). Also resolved: the six runtime units being `disabled` is BY DESIGN (03-03-SUMMARY: Phase 8 pairing daemon starts `arlowe-*` after pairing) — not a bug, and it explains "0 failed units". NEW: #22 no persistent journal, so a recovery event leaves no forensic trace (`journalctl -b -1` -> "no persistent journal was found"); `/var/lib/arlowe/logs` on shared owner_state is the natural breadcrumb location. NEW: #23 ADR-0005 says `arlowe-ab` rewrites `config.txt` but the code and ops doc say `cmdline.txt` — doc defect, must be corrected before Phase 9 OTA builds against the ADR. **SC status: SC1 PASS, SC2 layout PASS, SC2 grow PASS, SC3 PASS.**

**UPDATE 2026-09-08 (VALIDATION BUILD — clean, unpatched, flashed).** Rebuilt from the repo with F6's pin and the #21 fix, no hand-patching anywhere. **First attempt FAILED — and the new guard is what caught it:** `install-arlowe-cli.sh` aborted on `no such CLI target: /opt/arlowe/runtime/cli/face`. Not a missing file — an ORDERING defect: `install-arlowe-cli.sh` ran at step 5, BEFORE the runtime tree was rsynced into `/opt/arlowe/runtime`, and the old code documented this as intentional ("The targets are populated below"). **That ordering IS the mechanism behind #21** — creating symlinks before their targets exist means `ln -sf` has nothing to validate against, so a wrong name cannot be caught and ships as a dangling link. Fixing only the filename would have left the blind spot for the next CLI anyone adds. Fixed in `d557116`: CLI symlink install moved AFTER the runtime rsync, steps renumbered to match execution. **Second build CLEAN in 25 min, every gate green:** pi-gen re-cloned + pinned at `2026-06-18-raspios-bookworm-arm64` (F6 exercised from the repo, not arlowe-1's local tree); `All 13 declared packages present in rootfs`; `installed 9 symlinks` with the guard ACTIVE; all 9 verified to resolve inside the rootfs incl. `arlowe-ab -> /opt/arlowe/runtime/cli/ab`. Correct 5-partition layout. Flashed to /dev/sda via bmaptool, 22m07s @ 8.0 MiB/s, clean. NOTE a self-inflicted false alarm: my first symlink check reported all 9 DANGLING because absolute links resolved against arlowe-1's own root instead of the rootfs prefix — bad test, not a bad build; re-tested correctly. **AWAITING OWNER: final boot** — card into arlowe-1's SD slot, `sudo arlowe-ab status` with NO symlink patching. If it prints the slot, all four checkpoint SCs stand against an image built end-to-end from the repo and Phase 6 closes without an asterisk. **THEN: the bookkeeping.** `feat/110-arm64-ci-flash` still holds the entire image pipeline; `main` has none of it, while STATE/ROADMAP have claimed since June that it merged via PRs #112-#116. Open a PR after the boot confirms.

**UPDATE 2026-09-09 — VALIDATION BOOT PASSES. Phase 6 checkpoint closes without an asterisk.** Booted the clean repo-built image: `sudo arlowe-ab status` -> `Persistent default: slot A (root=PARTUUID=8096e9f9-5ac4-44d2-afe5-3ebe979ba085)` with **NO symlink hand-patching** — F7 #21 fixed at source and proven on an image built end-to-end from the repo (fresh pi-gen clone at the pinned tag, package guard green, CLI guard active). `sudo df -h /opt/arlowe/models` -> `/dev/mmcblk0p5 48G 6.2G 40G 14%` — SC2 grow clean on this image. `arlowe-firstboot` all THREE processes `status=0/SUCCESS` incl. the `.firstboot-done` ExecStartPost (one more green step than the hand-patched card showed). PARTUUIDs differ from the prior card because this is a fresh image — expected. **COVERAGE NOTE (do not overclaim):** the SC3 *flip mechanism* (switch B -> slot-B recovery -> self-heal to A) was proven on the hand-patched card; the *CLI shipping working* is proven on this clean card. Both halves are established but no single card has done both end to end — re-running `arlowe-ab switch B` on this card would close that last inference gap in ~2 min. **NEW FINDING #24:** `boot-check --first-boot` prints `Results: 0 passed, 14 failed / Some services need attention` on a device that is behaving CORRECTLY — the failures are the deferred NPU (`FAIL Axera NPU not responding`) and audio (`capture=FAIL device=none playback=FAIL`) plus the six runtime units that are SUPPOSED to be disabled until pairing. A shipped unit tells its owner everything is broken while sitting in its correct pre-pairing state. `boot-check` needs an expected-state notion, most naturally keyed on whether `/etc/arlowe/config.yml` exists (already the pairing signal). **NEXT: open the PR for `feat/110-arm64-ci-flash` -> `main`.** The entire image pipeline still lives only on that branch while STATE/ROADMAP have claimed since June that it merged via PRs #112-#116.

**CORRECTION 2026-09-09 (late) — SC3 did NOT run on the clean card.** `arlowe-ab switch B` was issued but did not take effect. Forensics from the card read in the USB reader on arlowe-1: `dumpe2fs -h /dev/sda3` (slot B) shows `Mount count: 1`, `Last mount time: Tue Sep 8 23:03:55` — the single mount performed by `partition-image.sh` at image-creation time. **Slot B has never booted on this card.** `cmdline.txt` still reads `root=PARTUUID=8096e9f9...` (slot A), so the flip never wrote slot B's PARTUUID either. Slot A shows mount count 4, owner_state 2, both consistent with the validation boots only. The WhisPlay face the owner saw was arlowe-1's OWN dev deployment (238G card), not the slot-B recovery face — the Pi OS desktop on the monitor confirms it, since the arlowe image is Lite and has no desktop. My earlier reading of that face as the recovery face was an inference from timing and was WRONG. **Standing SC3 coverage is therefore unchanged from 2026-09-08:** the flip mechanism (switch B -> slot-B recovery -> self-heal to A) is proven ONLY on the hand-patched card; the CLI shipping in working order is proven on the clean card. Both halves hold; no single card has run both. Closing that gap needs the test card back in the SD slot and one `arlowe-ab switch B` — low urgency, since both halves are independently established, but do NOT record SC3 as passing on a clean image until it does.

**UPDATE 2026-09-10 — SC3 IS BROKEN AND IT IS NOT OUR CODE (F7 #25).** Got SSH onto the test image over ethernet (192.168.1.190, user `pi`, Mac key installed) and reproduced the A/B flip failure a dozen times with instrumentation. **`arlowe-ab` is exonerated:** a plain `sed -i` on `cmdline.txt`, with the CLI not involved and /boot/firmware left mounted rw, reverts identically (written 22:25:02 -> reboot 22:25:03 -> rewritten to slot A 22:25:32). Something in the PLATFORM rewrites `root=` in cmdline.txt to the PARTUUID of the partition that actually booted. Only that file is affected — a marker file written to the same FAT partition survived the reboot untouched, as did config.txt.bak, so the partition is not rolled back. Nothing changes it while running (200s idle watch, zero change). The rewrite lands 29-38s after the reboot command, always before the kernel's first journal entry. NOT caused by `tryboot_a_b=1` (disabling it changed nothing). The PARTUUID map is correct (lsblk confirms p3=bb382f2e). Ruled out by measurement: page cache, systemd shutdown hooks, cron/timers, arlowe-recovery (not installed in slot A), imager_fixup (gated; no initramfs unpacked), wifi-check, grow-models, EEPROM config. **Mechanism still unidentified — do NOT write a fix.** Next session starts at: unit `ExecStop=` lines touching /boot/firmware, full `raspberrypi-sys-mods` contents, power-cycle vs reboot, and enabling persistent journald ([[F3]]) since every conclusion tonight had to be reconstructed from file mtimes. **Design implication:** editing a shared cmdline.txt may simply be the wrong mechanism; the Pi-native approach is `autoboot.txt` with real `boot_partition=` entries — the partition-level style ADR-0005 rejected. That is an ADR decision, not a patch. **SC STATUS CORRECTED: SC1 PASS, SC2 layout PASS, SC2 grow PASS, SC3 FAIL (never worked on any image).** The 2026-09-08 "SC3 PASS" record was wrong — it rested on two observed reboots plus the default reading slot A, both equally consistent with the flip never taking effect. Phase 6 is NOT complete.

**UPDATE 2026-09-10 23:06 — F7 #25 MECHANISM IDENTIFIED: THE PI BOOTLOADER REWRITES cmdline.txt.** Enabling persistent journald (the image ships `Storage=volatile`, finding #27) made the shutdown window observable and cracked it in one reboot. Timeline: cmdline.txt set to slot B 23:05:53 (verified on disk) -> `journald: Journal stopped` 23:05:54.557 (Linux fully down) -> **cmdline.txt rewritten to slot A at 23:06:24** -> next kernel 23:07:44. The write lands 30s after Linux shut down and 80s before the next kernel started, so NO Linux process was alive; only the Pi bootloader runs in that window. The firmware rewrites `root=` to the partition it actually booted. This explains everything: arlowe-ab and plain sed behaving identically, the write provably reaching disk then reverting, only cmdline.txt being touched while a marker file survives, no change during a 200s idle watch, and slot B never mounting (the firmware never boots it, so the recovery stub never runs). **arlowe-ab is CORRECT — do not fix it. The mechanism it implements is the problem.** **ADR-level decision required:** editing a shared cmdline.txt cannot persist an A/B default on this platform. The Pi-native answer is `autoboot.txt` with real `boot_partition=` entries — the partition-level style ADR-0005 rejected in favour of a single shared /boot, which would also change ADR-0004's layout and SC2. Alternatives: drive the flip from bootloader config the firmware does not overwrite, or drop in-place A/B for v1 and reach slot B only via explicit `reboot 0 tryboot`. **SC3 remains FAIL and is now understood.** Phase 6 is NOT complete. Test card left at 192.168.1.190 (ethernet, user pi, Mac key installed), journald flipped to persistent, cmdline.txt on slot A, config.txt intact.

**UPDATE 2026-09-10 (late) — PR #117 OPENED; the "main-branch discrepancy" blocker was FALSE.** Pushed feat/110 (22 commits) and opened https://github.com/focal55/arlowe-firmware/pull/117 against main. **Correction:** `origin/main` has had the whole Phase 6 image pipeline since #116 merged 2026-06-14 — `flash-sd.sh`, `build-image.sh`, `partition-image.sh` are all there. The July blocker claiming "the entire pipeline lives only on feat/110" was written from a check against the STALE LOCAL `main` (3 commits behind at 758483a, which genuinely lacks those files). The real delta is 20 files / 899 insertions of checkpoint FIXES, not a phase of missing work. I repeated that false claim several times this session before verifying it. PR body states SC3 as known-failing with the bootloader root cause, and explains that merging with a documented failing criterion is deliberate — the alternative is holding four verified fixes hostage to an ADR decision. Awaiting owner merge (pr-reviewer cannot self-approve on this board).

**UPDATE 2026-09-10 23:30 — TRYBOOT ALSO FAILS (#28). Both A/B mechanisms disproven; no ADR written.** Owner approved "slot B = tryboot-only for v1"; I tested it before writing it up and **it does not work**. `reboot "0 tryboot"` and `systemctl reboot "0 tryboot"` (systemd confirmed accepting the argument) both left slot B's mount count at 1 and root on p2. A planted marker in `tryboot_cmdline.txt` did NOT reach `/proc/cmdline`, proving **`tryboot.txt` is never read** — unlike `cmdline.txt`, which the firmware reads and then overrides (#25). Bootloader EEPROM `57db150d` (2025-11-05, update available); tryboot support predates it, so age is not an obvious cause. **Scoreboard: cmdline.txt persistent flip = FAILS (#25); tryboot one-shot = FAILS (#28); autoboot.txt + two FAT boot partitions = UNTESTED and would require ADR-0004 layout + SC2 rewrite.** Slot B has never booted on any image by any method. **BLOCKING INSTRUMENT: a USB-TTL serial console.** `BOOT_UART=1` is already set, so the bootloader prints which config file it loads and whether it honours tryboot; every conclusion so far was inferred from file mtimes and ext4 mount counts because the bootloader phase is otherwise invisible. July's plan called for a serial adapter and never used it. **No ADR written — two mechanisms disproven, third untested; an ADR now would record a decision with no verified mechanism.** Test card at 192.168.1.190, marker removed, cmdline.txt on slot A, tryboot_cmdline.txt restored.

**UPDATE 2026-09-10 (late) — PHASE 7 PLANNED.** `/gsd:plan-phase 7` complete on branch `plan/phase-7-device-identity` (research `7381902`, plans `f8f8f72`, revisions `afdf226` + `7c74b45`). 11 plans / 7 waves, verification PASSED with 0 blockers after 3 checker iterations and 2 revision passes. **Owner decision recorded:** managed PKI = **AWS IoT Core native issuance** (Amazon root CA, `CreateCertificateFromCsr`) — AWS Private CA explicitly ruled out at $400/mo; Google Cloud IoT Core is retired (2023-08-16); Let's Encrypt is a category error (server certs for public DNS names, not client certs for NAT'd devices). **Bootstrap = token-agnostic:** `arlowe-identity provision --ca-broker-url --owner-token`; the owner-account question is DEFERRED to Phase 8, so a hand-minted token and a future account-system token both work unchanged. No claim certificates (one image on every unit means any baked secret is on every unit). **IDENT-02 splits Phase 7 + Phase 8** — the device-ID half lands now, the customer-account binding cannot until pairing exists; traceability row updated rather than ticked prematurely. AWS certs expire 2049 so there is no renewal daemon; revocation is server-side at every auth so the device carries no CRL/OCSP code. **8 warnings remain as execution notes, none blocking** — most important: W3 (task 2's live-run command omits `AWS_PROFILE`/`AWS_REGION`, which would fail mid-run after billing resources are created), W1 (the dry-run's ordering backstop is mocked at the wrong layer and does not actually catch a hoisted revocation), W2 (step 8 has no propagation tolerance, so a slow AWS status change reports a false "SC4 failed"), W4 (revoke.sh stdout pollutes the probe's JSON return). W5/W6 are understated line budgets on 07-06 and 07-04. **THREE CI GATES FOUND BROKEN OR MIS-SET THIS WEEK, all silent:** #119 dashboard rendered-text gate failing on main since 2026-06-14; #120 every Node job skipped because `has_node` checks a nonexistent ROOT package.json (dashboard lint/typecheck/test/build have never run); #121 PR size hard cap still the Phase-1 value of 1500 with a comment naming its own expiry, five phases overdue. Fixing #120 turns `lint` red immediately — 5 verified `react-hooks/set-state-in-effect` errors — so 07-02 sets `continue-on-error` on lint only and names the five files to keep a dashboard cleanup out of Phase 7.

**UPDATE 2026-09-10 — PHASE 7 EXECUTION STARTED: 07-01 COMPLETE.** Plan 07-01 executed on `plan/phase-7-device-identity`, 3/3 tasks, zero deviations. **ADR-0007** (`docs/architecture/0007-managed-pki-service-selection.md`, 229 lines) records AWS IoT Core native Amazon-root issuance via `iot:CreateCertificateFromCsr`, rules out AWS Private CA with the $400/mo figure and names the ~$4,800/year trap explicitly, rules out GCP IoT Core (retired), ACME (category error), step-ca/Vault (operational custody), Smallstep Certificate Manager (sales-gated pricing), with Azure IoT Hub + DPS as runner-up. Lifecycle: broker-mediated issuance, **renewal = none** (AWS IoT certs run to 2049-12-31), revocation by `update-certificate --new-status REVOKED` checked server-side, device carries no CRL/OCSP. **Revocation bound has a number:** `credentialDurationSeconds=900` <= `identity.poll_interval_seconds` (min 900, default 3600) collapses `max(poll_interval, remaining_credential_lifetime)` to one polling interval — that is what SC4 tests. No claim certs. Token-agnostic bootstrap; owner-account half of IDENT-02 deferred to Phase 8. **Status is Proposed, not Accepted** — the "provisioning and the credentials provider are unbilled" claim is inference from absence on a pricing page, not an AWS statement; 07-09 amends after a staging run and a real bill. **`identity` config block added as an OPTIONAL top-level key** (`provisioning_url`/`credentials_endpoint`/`role_alias` all `""`, `poll_interval_seconds: 3600`), deliberately NOT in schema `required` because `save-body.ts` hard-codes an 8-key `REQUIRED_KEYS` and a 9th required key would 422 every dashboard audio save; presence is guaranteed by `defaults.yml` + deep-merge instead. 7 new tests (4 Python, 3 dashboard) prove the block is inert; the dashboard three fail if anyone promotes `identity` to required without touching `save-body.ts`. **Caveat: those 3 dashboard tests do not run in CI until 07-02 repoints `ci.yml`'s `detect` at `runtime/dashboard` (#120)** — they are local-only today. Commits `58366ee`, `f3f7a5c`, `ed1e403`. SC1 and IDENT-01 satisfied. **Note: 07-02 was executing concurrently in the SAME working tree** — every commit staged files by explicit path; the "parallel dispatch needs git worktrees" hazard was live throughout.

**UPDATE 2026-09-10 — 07-02 COMPLETE: CI ACTUALLY RUNS TESTS NOW (#120 half-closed).** Plan 07-02 executed on `plan/phase-7-device-identity`, commits `7750d32` + `7adab79`. **The finding that mattered: no test job of any kind had ever run on a pull request.** `ci.yml`'s `detect` job gated `lint`/`typecheck`/`test`/`build` on a ROOT `package.json` that does not exist, and there was no Python job at all — so Phases 4-6's entire pytest suite and every dashboard test were laptop-only. Fixed: (1) new **`python-test`** job, ungated, `python -m pytest runtime/lib/tests/ -q` on python 3.11 with `PYTHONPATH=runtime/lib ARLOWE_SCHEMA_PATH=config/schema.yml ARLOWE_DEFAULTS_PATH=config/defaults.yml ARLOWE_CONFIG_PATH=/nonexistent` (later plans copy this block verbatim); (2) new **`python-floor-bookworm`** job — same suite inside `container: debian:bookworm` against **apt** `python3-cryptography`/`python3-requests`, measured at **cryptography 38.0.4 / requests 2.28.1**. This is the durable enforcement of the API floor 07-06 depends on; the job name is load-bearing, do not rename it. apt, not a pip pin, because `/opt/arlowe/venvs` is empty on the image and system python3 + apt is the real device surface; (3) `detect` repointed at `runtime/dashboard/package.json`, all four Node jobs given `working-directory: runtime/dashboard` + `cache-dependency-path`, `pnpm test` -> **`pnpm test:unit`** (`pnpm test` never existed), and a `typecheck` script (`tsc --noEmit`) added to `package.json` (that never existed either). 07-01's 3 dashboard tripwire tests now run on PRs. (4) Phase 7 deps declared on BOTH surfaces in one place so 07-06 and 07-07 no longer contend over `requirements.txt`: `cryptography==43.0.3` + `requests==2.32.5` pinned for CI/dev, `python3-cryptography` + `python3-requests` added to `pi-gen/stage-arlowe/00-packages/00-packages-nr` (build-image.sh's declared-package guard now asserts 15 packages at the next image build). **2 deviations, both auto-fixed:** [Rule 3] the plan specified pnpm 9, but `runtime/dashboard/pnpm-workspace.yaml` carries settings-only keys (`ignoredBuiltDependencies`) with no `packages` key and **pnpm 9 aborts outright** with `packages field missing or empty` — all four Node jobs pinned to **pnpm 10** instead (`node_modules/.modules.yaml` records pnpm@10.15.0); [Rule 1] a comment overclaiming the floor job's apt list as a superset of `00-packages-nr` was corrected (python3-rpi.gpio/spidev are Pi-only). **#120 stays OPEN for its residue:** `lint` is `continue-on-error: true` against 5 pre-existing `react-hooks/set-state-in-effect` errors (`app/page.tsx:44`, `app/logs/page.tsx:53`, `app/components/RetroActivityMonitor.tsx:56`, `app/connectivity/components/NetworkList.tsx:58`, `app/connectivity/components/SavedNetworksList.tsx:98`) — `continue-on-error` and not an `if:` skip, because `build` declares `needs: [detect, lint, typecheck, test]` and a skipped dep skips the dependent. `typecheck`, `test` and `build` are BLOCKING and verified green. `test:e2e` still not wired (needs browsers + a running server). Task 2 (clean-checkout hygiene) needed **no code change** — all 15 fixture leaf dirs already carry a tracked `.keep`; a fresh clone passes 85/85. **All verification is LOCAL reproduction of the job steps — these jobs have never executed on GitHub Actions.** The first PR against `main` is the real proof; watch `pnpm/action-setup@v4` running from a repo root with no `package.json`.

Progress: Phase 1 [██████████] 100% qualified; Phase 2 [██████████] 100%; Phase 3 [██████████] 100%; Phase 4 [██████████] 100% (passed-with-notes); Phase 5 [██████████] 100% (passed-with-notes); Phase 6 [██████████] 100% (execution-complete pending hardware checkpoint); Phase 7 [██░░░░░░░░] 2/11 plans (07-01, 07-02 done)

## Performance Metrics

**Velocity:**
- Phase 1: 15 plans, ~3 days agent + 1 finalizer day
- Phase 2: 4 plans, 1 day (parallel fan-out of 02-01..02-03, then 02-04 SC4 cleanup)
- Phase 4: 4 plans, 1 day (04-01 schema/loader → 04-02 install/security → 04-03 dashboard write → 04-04 persona slice)

**By Phase:**

| Phase | Plans | Done | Status |
|-------|-------|------|--------|
| 1 | 15 | 15 | Complete (qualified) |
| 2 | 4 | 4 | Complete |
| 3 | 5 | 5 | Complete (passed-with-notes) |
| 4 | 4 | 4 | Complete (passed-with-notes) |
| 5 | 7 | 7 | Complete (passed-with-notes; on-Pi SC1-4 deferred) |
| 6 | 6 | 6 | Complete (execution-complete; on-Pi SC1-5 deferred to hardware checkpoint) |
| 7 | 11 | 1 | In progress (07-01 done) |

**Recent Trend (Phase 6):**
- 06-01: ADR-0004 (partition sizing) + ADR-0005 (tryboot root= A/B selector) + ADR-0006 (Whisper model) + sizing reconciliation
- 06-02: SHA-pinned model + WhisPlay manifest/verify gate (#112)
- 06-03: pi-gen stage-arlowe chroot provisioning, model-free rootfs (#113)
- 06-04: build-image.sh measure-then-set 5-partition A/B + models grow-to-fill (#114)
- 06-05: tryboot root= selector + arlowe-ab flip CLI + slot-B recovery stub (#115)
- 06-06: arm64 CI + shellcheck gate + flash-sd.sh + dev-deploy.sh + runbook (#116) — closes Phase 6

## Accumulated Context

### Decisions

Decisions are logged in PROJECT.md Key Decisions table.
Recent decisions affecting current work:

- **ADR-0001** (resolved): option-2 chosen for `openai_wrapper.py`. Router points at ax-llm `/api/chat` native (`localhost:8000`), not `/v1/chat/completions`. `qwen-openai.service` deprecated.
- **ADR-0002** (resolved): `arlowe-scheduled-summary.service` stripped from firmware.
- **ADR-0003** (resolved, Phase 4): loosen-perms for dashboard→/etc/arlowe/config.yml write; `ota.channel_url` + `support_mode.*` flagged for Phase 10 re-hardening.
- **Phase 4 schema** (resolved): one `config/schema.yml` (JSON-Schema-in-YAML), validated by jsonschema (Python) + ajv (dashboard). SC4 live knob = persona only; other CONFIG-06 knobs defined+validated, consumed in Phases 7–11. ExecStartPre `arlowe_config_validate` on the three python-bearing units (arlowe-face, arlowe-voice via voice venv; qwen-tokenizer via llm venv); qwen-api.service untouched (native, no venv).
- **Phase 4 SC4 deferral** (resolved): on-device persona-knob end-to-end check deferred to Phase 6/12 — arlowe-1 has no arlowe layout to run it; mirrors Phase 1 SC4 → Phase 12 and Phase 3 SC4 → staging.
- **Phase 2 sanitization gate** (resolved): three CI jobs (banlist-and-units, self-test, dashboard-rendered-text) gate every PR. `.sanitize-allowlist` codifies exception paths.
- **ADR-0007** (Proposed, Phase 7): managed PKI = AWS IoT Core native Amazon-root issuance via `iot:CreateCertificateFromCsr`. AWS Private CA ruled out at $400/mo. No renewal (certs run to 2049); revocation is server-side with a one-polling-interval bound (`credentialDurationSeconds=900` <= `identity.poll_interval_seconds`). No claim certs; bootstrap is token-agnostic. Flips to Accepted in 07-09 after a staging run and a real bill.
- **Bookworm `cryptography` 38.0.4 is a hard API floor for `runtime/lib`** (Phase 7, 07-02): enforced by the `python-floor-bookworm` CI job, which runs the suite in a `debian:bookworm` container against **apt** `python3-cryptography` (38.0.4) / `python3-requests` (2.28.1) — not a pip pin, because the image has no venv and runs system python3. No `x509.verification`, no `not_valid_after_utc`, no other 42+-only API. The `cryptography==43.0.3` pin in `runtime/lib/requirements.txt` governs CI/dev only.
- **CI pins pnpm 10, not 9** (Phase 7, 07-02): `runtime/dashboard/pnpm-workspace.yaml` holds settings-only keys with no `packages` field, which pnpm 9 rejects with `packages field missing or empty`.
- **`lint` is non-blocking, `typecheck`/`test`/`build` are blocking** (Phase 7, 07-02): 5 pre-existing `react-hooks/set-state-in-effect` errors waived via `continue-on-error` (NOT an `if:` skip — `build` needs `lint`), recorded on #120 with file:line for a dedicated dashboard plan.
- **`identity` config block is OPTIONAL, never `required`** (Phase 7, 07-01): `config/schema.yml`'s top-level `required` stays at 8 entries because `runtime/dashboard/app/audio/save-body.ts` hard-codes them as `REQUIRED_KEYS` and `POST /api/config` 422s partial bodies. Presence in the merged dict is guaranteed by `defaults.yml` + deep-merge. Any future config block must follow this pattern unless the dashboard is updated in the same change.
- **WhisPlay driver**: PiSugar (Apache 2.0). Strategy A — vendor at image build (Phase 6).
- **ax-llm submodule**: pinned at `df75c34c…` on `axcl-context` branch.
- **axcl deb**: SHA-256 pinned in `third_party/axcl/manifest.yml`, never committed (Strategy C). `scripts/verify-third-party.sh` gates.
- **Phase 3 layout** (resolved): `arlowe` system user (uid<1000, HOME=/var/lib/arlowe, nologin); code root-owned at `/opt/arlowe`, state at `/var/lib/arlowe`; six system-level units with PrivateTmp/ProtectSystem=strict/ReadWritePaths sandboxing; CLI symlinks at `/usr/local/sbin/arlowe-*`; udev (gpio/spi/axera) + polkit rules. SC4 verified on real hardware.
- **Phase 3 group set** (resolved #73): the `arlowe` *user* gets supplementary groups {audio, gpio, spi}; video+dialout dropped. **Residual debt (#78):** `units/arlowe-face.service` still declares `SupplementaryGroups=...video` + `DeviceAllow=/dev/fb0` — #73 missed the unit file. Open backlog bug, NOT auto-dispatched.
- **Axera NPU perms** (resolved #75): nodes 0660 root:arlowe; `install-arlowe-udev-polkit.sh` removes the axcl deb's broken `GROUP="<users>"` rule. Runtime verification deferred to Phase 6.

### Pending Todos

In `.planning/todos/pending/`:
- F1-port-8080-env-override.md — Phase 5-adjacent (face_service.py hardcoded port)
- F2-vendor-whisplay-driver.md — Phase 6 (image build) — CONFIRMED STILL OPEN: it is the active build blocker for the hardware checkpoint (verify-third-party hard-FAILs without WhisPlay.py). Was wrongly assumed satisfied by 06-03.
- F3-arlowe1-persistent-journald.md — workforce infra (dev-env)
- F4-plan-13-rerun-post-phase-6.md — post-Phase-6 hybrid smoke-test re-run
- F6-pin-pigen-version.md — NEW 2026-07-08. pi-gen unpinned (master=trixie), forced a manual keyring patch to build bookworm; pin it for reproducibility

Phase 5 follow-up chores (GitHub backlog, p2):
- #102 — dashboard CONFIG_DEFAULTS duplicates config/defaults.yml; single-source it
- #103 — auto_collect.py docstring missing ARLOWE_PLAY_DEVICE
- #94 SC1-SC4 on-Pi verification — DEFERRED to a hardware checkpoint (runbook: docs/operations/phase-5-audio.md)

In `.planning/todos/done/`:
- F5-adr-0001-why-option-2-internal-contradiction.md — closed by Plan 02-04 (ADR doc rewritten)

Workforce-infra debt tracked in Claude's memory store:
- Board-sync workflow needs PAT with `Projects: read/write` scope (`PROJECTS_TOKEN` secret) — owner must mint.
- pr-reviewer agents can't merge on the user ProjectV2 — Joe merges manually.

### Blockers/Concerns

- **[RESOLVED 2026-07-08] Phase 6 checkpoint build blocker (WhisPlay):** verify gate needs WhisPlay.py + Apache LICENSE co-located; staged `~/whisplay-staging/` on arlowe-1, verify green. Provenance gap (dev copy != upstream HEAD) tracked in F2, non-blocking for checkpoint.
- **[RESOLVED 2026-07-08] SD-card size:** owner has a 64 GB card → `CARD_SIZE_GB=32` (default kept; same-nominal-card bug doesn't bite with headroom).
- **[FALSE — RETRACTED 2026-09-10] main-branch discrepancy:** This blocker was WRONG. `origin/main` has had the full Phase 6 image pipeline since #116 merged 2026-06-14 (`scripts/flash-sd.sh`, `build-image.sh`, `partition-image.sh` all present). The claim came from checking the STALE LOCAL `main` branch, which is 3 commits behind at `758483a` and genuinely lacks those files. Same failure class as F7 #16-#27: a claim recorded from a command that did not test what it appeared to. Lesson: compare against `origin/main`, not `main`. Original text follows. ~~ STATE/ROADMAP claim Phase 6 "merged via PRs #112–#116," but `git log main..HEAD` shows the ENTIRE Phase 6 image pipeline (build-image.sh, flash-sd.sh, partition-image.sh, 3094 insertions) lives only on branch `feat/110-arm64-ci-flash` — `flash-sd.sh` does NOT exist on `main`. Bookkeeping to reconcile post-checkpoint (merge the branch or correct the "merged" claims). Does not block the checkpoint (arlowe-1 has the branch code).~~
- **Latent bug (file after checkpoint):** `CARD_SIZE_GB` is labeled "GB" but computed as `* 1024^3` (GiB) in `scripts/lib/partition-image.sh:136`, and the `.img` is `truncate`'d to the full card size with p5 ending at 100%. So a same-nominal card always fails (a real 32 GB card is ~29.8 GiB < a 32 GiB image → flash overruns + corrupts the models partition). Fix: interpret as decimal GB, or size the image to partitions-only and grow on first boot.
- arlowe-1 has no working GitHub auth (SSH publickey denied; gh token expired) — private-repo work must rsync from the Mac. Durable infra debt.
- ADR pending (Phase 7): specific managed-PKI service selection (owner leaning AWS IoT).
- ADR pending (Phase 8): pairing channel mechanism (Wi-Fi captive portal vs. BLE).
- **The new CI jobs have never run on GitHub Actions** (07-02): `python-test`, `python-floor-bookworm` and the four repointed Node jobs were verified by locally reproducing each step (clean venv, fresh clone, OrbStack `debian:bookworm` container, pnpm 10). The first PR opened against `main` is the first real execution. Watch `pnpm/action-setup@v4` invoked from a repo root that has no `package.json`.
- **A green CI run does not mean eslint-clean** (#120 residue): `lint` is `continue-on-error` until the 5 dashboard `set-state-in-effect` errors are fixed.

## Session Continuity

Last session: 2026-09-10 (Phase 7 execution — plans 07-01 and 07-02 complete)

Stopped at: Completed 07-02-PLAN.md (CI enforcement + Phase 7 python deps on both surfaces). Summary: `.planning/phases/07-device-identity-and-pki/07-02-SUMMARY.md`. Branch `plan/phase-7-device-identity`, commits `7750d32` / `7adab79`. Resume file: None.

Next for Phase 7: wave 2 onward. 07-06 and 07-07 can now run in parallel — `runtime/lib/requirements.txt` is final for the phase, so they no longer write-conflict. Any Python test they add runs on PRs automatically via `python-test` AND under the bookworm floor.

Previously: Completed 07-01-PLAN.md (ADR-0007 + optional `identity` config block). Summary: `.planning/phases/07-device-identity-and-pki/07-01-SUMMARY.md`. Branch `plan/phase-7-device-identity`, commits `58366ee` / `f3f7a5c` / `ed1e403`. Resume file: None.

Next for Phase 7: 07-02 (CI teeth — python-test job, bookworm cryptography floor job, dashboard jobs repointed at `runtime/dashboard` to close #120). 07-02 was already running concurrently in this same working tree while 07-01 executed; check `git log` before assuming its state.

Still open and unrelated to Phase 7: the Phase 6 hardware checkpoint (SC3 FAILS — the Pi bootloader rewrites `cmdline.txt`, F7 #25/#28; no A/B mechanism verified) and PR #117 awaiting owner merge.

### Historical: 2026-06-19 (Phase 6 hardware checkpoint — first-ever image build — set up and PAUSED mid-flight)

### Why this checkpoint, now (decision rationale)
Run the deferred on-Pi flash+boot checkpoint BEFORE starting Phase 7, while the owner is physically at the Pi. Phase 6 is the first phase that produces a flashable artifact (earlier on-Pi SC deferrals from Phases 1/4/5 were forced — there was no image and arlowe-1 has no real arlowe layout). Phase 7 is the first phase whose deliverables physically depend on the Phase 6 substrate (cert/key written to the `/var/lib/arlowe` owner-state partition; device-id derived from real CPU serial), and PKI work is outward-facing + hard to unwind. So validate the substrate before building PKI on it. This checkpoint also absorbs the deferred Phase 4 SC4 + Phase 5 SC1-4 (#94) runs.

### Scope of THIS run (spare card, NO peripherals attached)
Validates: SC1 boot-to-ready-to-pair, SC2 five-partition layout + models grow-to-fill, SC3 A/B tryboot flip + slot-B recovery over SERIAL + default-reset (Whisplay-display half deferred), SC4 dev-deploy + recovery-doc. Costs nothing for the Phase-7 goal. STILL DEFERRED (need peripherals; irrelevant to Phase 7): Phase 5 audio SC1-4 (no USB audio), Whisplay-display half of SC3, Phase 4 persona visual, AX/LLM path.

### State on arlowe-1 (build host — all PERSISTS across the pause)
- Repo: `~/projects/arlowe-firmware` (rsynced from Mac; on branch feat/110 but content == origin/main @ 9206c63). arlowe-1 has NO working GitHub auth (SSH publickey denied, gh token expired) — that's why we rsync from the Mac instead of cloning.
- Upstream pi-gen (arm64) cloned + overlaid into `pi-gen/` (build.sh + stage0-5 present, repo's config + stage-arlowe preserved). pi-gen native build path (`cd pi-gen && sudo ./build.sh`); Docker NOT used (arlowe-1 is arm64). sudo is passwordless on arlowe-1.
- apt build deps installed (quilt parted debootstrap zerofree dosfstools e2fsprogs libarchive-tools cloud-guest-utils ripgrep python3-yaml qemu-user-static binfmt-support etc.).
- Models cache staged REAL files at `~/arlowe-models-cache` (6.2G): `qwen2.5-7b-int4-ax650/` (from ~/models/Qwen2.5-7B-Instruct/qwen2.5-7b-ctx-int4-ax650), `piper-voices/{onnx,json}`, `whisper/small.en/` (faster-whisper-small.en fetched from HF Systran/faster-whisper-small.en, model.bin 462M). 02-models uses `rsync -a` so they MUST be real files, not symlinks.
- AXCL deb at `~/axcl/axcl_host_aarch64_V3.10.2.deb` (sha matches manifest).
- WhisPlay driver present at `~/Library/Whisplay/Driver/WhisPlay.py` (NOT yet staged into the repo — see blocker below).

### verify-third-party.sh result (captured 2026-06-19)
PASS: axcl deb sha ✓, ax-llm @ df75c34c ✓. WARN (TODO_SHA256 placeholders — record in third_party/models/manifest.yml for issue #106): qwen2.5-7b-int4-ax650 `5aee91b3fc83994a59a197195690b7256922007cb897d2ecfbcc1665e4f37f17`, faster-whisper-small.en `92c48d42979891ca9a9498df22ff436ab58d45328543f6d19307d03e88d9491f`, piper-en_US-lessac-medium `5efe09e69902187827af646e1a6e9d269dee769f9877d17b16b1b46eeaaf019f`. WARN (non-blocking): WM8960 HAT redistribution rights unresolved. **HARD FAIL: WhisPlay.py not found** — gates the build.

### RESUME CHECKLIST (do these in order)
1. DECIDE SD card size, then set CARD_SIZE_GB to match (build NOT yet launched, so free to change): 64 GB+ card → keep CARD_SIZE_GB=32; only a 32 GB card → CARD_SIZE_GB=16. A nominal 32 GB card does NOT fit a CARD_SIZE_GB=32 build (see latent bug in Blockers). 16 GB card unusable.
2. STAGE WhisPlay driver (clears the verify FAIL): `cp ~/Library/Whisplay/Driver/WhisPlay.py ~/projects/arlowe-firmware/third_party/whisplay-driver/` OR run build with `ARLOWE_WHISPLAY_SRC=~/Library/Whisplay/Driver`. NOTE: third_party/whisplay-driver/INSTALL.md pins a specific PiSugar/Whisplay commit — verify the local copy matches it, else clone PiSugar/Whisplay at the pinned commit for provenance.
3. RE-RUN verify green: `cd ~/projects/arlowe-firmware && AXCL_DEB=~/axcl/axcl_host_aarch64_V3.10.2.deb ARLOWE_MODELS_DIR=~/arlowe-models-cache ARLOWE_WHISPLAY_SRC=~/Library/Whisplay/Driver bash scripts/verify-third-party.sh` (WhisPlay FAIL should clear; TODO/WM8960 WARNs are expected/non-blocking).
4. BUILD (first-ever; expect snags), detached + logged on arlowe-1: `cd ~/projects/arlowe-firmware && AXCL_DEB=~/axcl/axcl_host_aarch64_V3.10.2.deb ARLOWE_MODELS_DIR=~/arlowe-models-cache ARLOWE_WHISPLAY_SRC=~/Library/Whisplay/Driver CARD_SIZE_GB=<32|16> bash scripts/build-image.sh`. Output: build/arlowe.img + partition table. ~45-90 min+.
5. FLASH spare card: `scripts/flash-sd.sh build/arlowe.img /dev/<device> ` — confirm the device node by eye first (guard refuses non-removable disks; SD cards are mmcblk, guard preserves the controller digit). Record measured flash time.
6. PHYSICAL SC run: need a USB-TTL SERIAL console (GPIO UART, 115200) — it's the only window into slot-B recovery with no Whisplay. Power down arlowe-1, swap in spare card, boot → SC1/SC2. arlowe-1's own boot card stays safe (we flash the SPARE). SC3: arlowe-ab flip + reboot → slot B recovery on serial → confirm default reset to A. Restore arlowe-1's original card when done.
7. RECORD: fill phase-6-build-flash-deploy.md (flash time, partition sizes, SC results), close #106 TODO SHAs with the digests above, flip STATE/ROADMAP Phase 6 to fully complete, file the CARD_SIZE_GB bug, note remaining peripheral-deferred SCs.

After the checkpoint: `/gsd:discuss-phase 7` (device identity and PKI) — managed-PKI ADR is a real decision (owner leaning AWS IoT), discuss before planning.

Other backlog (unchanged, loop OFF): #78 (face-unit video/fb0, type:bug), #102 (dashboard CONFIG_DEFAULTS single-source), #103 (auto_collect.py docstring). F4 plan-13 hybrid smoke re-run is post-Phase-6.
